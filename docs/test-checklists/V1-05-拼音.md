# 功能名：V1-05 拼音标注

> 改动范围：`play/index.html`（pinyin-pro 预加载）、`play/pages/settings.html`（拼音开关）、`play/pages/quiz.html`（renderPinyin + showQuizScreen）、`play/service-worker.js`（CACHE_VERSION v2.8）

## 桌面浏览器测试（Windows + Chrome）

- [ ] 进设置页 → 顶部出现「🔤 显示拼音标注」拨动开关，默认开启（蓝色）
- [ ] 开关拨到关 → 刷新设置页 → 开关仍为关（localStorage 持久化）
- [ ] 开关拨回开 → 刷新 → 仍为开
- [ ] 打开一局游戏，进答题页：
  - [ ] 开关为开时：题干文字上方有拼音（ruby/rt 标注），字号约为主字 50%、颜色灰
  - [ ] 选项 A/B/C/D 的**文字部分**有拼音；A/B/C/D **字母** 无拼音（.option-label 不变）
  - [ ] 标点符号（句号、问号等）和数字/字母不带拼音
  - [ ] 关掉开关后进下一道题 → 纯文字，无 ruby 标注
- [ ] 答对/答错/保底逻辑不受影响；反馈区文字正常（用原始数据，非 DOM textContent）
- [ ] F12 Console 无红色错误（特别检查 pinyinPro 是否已定义）

## 窄屏测试（浏览器缩窄到 380px 宽模拟 iPad Mini）

- [ ] 开拼音时，长选项文字能换行，不截断，不溢出卡片边界
- [ ] 开拼音时，题干多行显示正常，无横向滚动条

## iPad Safari 测试

- [ ] 设置页拼音开关正常；默认开
- [ ] 答题页拼音标注显示正确（ruby 在 Safari 上支持良好）
- [ ] 长选项 + 开拼音 → 换行不截断，孩子能看完全文
- [ ] Eruda Console（?debug=1）无红色错误

## iPad PWA 主屏幕三态测试（仅游戏端必查）

- [ ] Safari 浏览器直接访问（有网）：拼音标注正常；设置开关正常
- [ ] PWA 主屏幕图标启动（有网）：同上；设置页 BUILD_TAG 显示 V1-05-拼音
- [ ] PWA 主屏幕图标启动（飞行模式）：pinyin-pro CDN 已由 SW v2.8 动态缓存，离线下拼音仍可显示
