# 功能名：V1-12 编辑端 L1 开场故事 + 结局故事字段

> 改动范围：`admin/pages/levels.html`（L1 表单新增两个 textarea）、`admin/scripts/level-form.js`（读写新字段）

---

## 桌面浏览器测试（Windows + Chrome）

### 表单显示

- [ ] 打开新建探险弹窗 → 出现「📖 探险故事（可选）」分组，含「开场故事」和「结局故事」两个输入框
- [ ] 打开编辑已有探险弹窗 → 同样出现两个输入框
- [ ] 两个输入框各有对应的小字说明

### 保存与回读

- [ ] 开场故事和结局故事都填内容 → 保存 → 重新打开编辑弹窗 → 两个值都正确回显
- [ ] 只填开场故事，结局故事留空 → 保存不报错 → 重开只有开场故事有内容
- [ ] 只填结局故事，开场故事留空 → 保存不报错 → 重开只有结局故事有内容
- [ ] 两个框都留空 → 保存不报错

### 老数据兼容

- [ ] MVP 阶段建的旧 L1（从未填过故事字段）→ 打开编辑弹窗不报错 → 两个框显示为空

### 新建探险

- [ ] 点「新建探险」→ 两个故事框均为空（不残留上次编辑的内容）
- [ ] 填故事后新建保存 → DevTools → IndexedDB → levels 表 → 能看到 openingStory / endingStory 字段

### F12 检查

- [ ] 整个操作过程 Console 无红色错误

---

## iPad Safari 测试

- [ ] 在 iPad Safari 上打开编辑端 → 新建/编辑 L1 弹窗中两个故事框正常显示
- [ ] 填写内容并保存 → 重开弹窗值还在
- [ ] Eruda Console（?debug=1）无红色错误
