# 功能名：M25 极简通关庆典页（victory.html）

## 前置条件
已导入探险 JSON，完整打完最后一站所有题目后自动跳转 victory.html。

---

## 桌面浏览器测试（Windows + Chrome）

### 基础显示

- [ ] 进入页面后显示 emoji 撒花动画（约 4 秒后自动停止，不报错）
- [ ] 🏆 大图标有弹出动画；显示「通关啦！」+ 探险名
- [ ] 成绩卡显示三行：总分 / 答对题数 / 用时

### 总分验证（手动核对）

- [ ] 逐题核算：一次对=10分、错1次再对=5分、用求助=3分、三次错=0分
- [ ] 成绩卡总分与手算结果一致

### 用时验证

- [ ] 用时显示为正常的分钟数（如「3 分 27 秒」），不是 0 秒、负数或 NaN
- [ ] F12 → IndexedDB → `sessions` → `endedAt` 有值（非 null）、`status === "completed"`

### 刷新不重算

- [ ] F12 记下 `endedAt` 值 → 刷新通关页 → `endedAt` 保持相同（结算幂等）
- [ ] 用时显示不变（没有被刷新拉长）

### 合影区

- [ ] 拍了 N 张合影 → 标题显示「📷 这次的合影（N 张）」，横向可滚动
- [ ] 每张合影下方小字：「第 X 站 · 点位名」，X 和点位名与实际站号匹配
- [ ] 点击缩略图 → 全屏遮罩弹出，显示大图 + 「长按图片保存到相册 · 点击关闭」
- [ ] 点击全屏遮罩任意位置 → 关闭全屏
- [ ] **故意跳过部分站拍照**：只显示拍了的站，跳过的站不出现；站号仍正确

### 一张合影都没拍

- [ ] 显示「这次没有拍合影哦，下次记得拍！」，不报错

### 返回首页

- [ ] 「🏠 返回首页」按钮跳转到 `play/index.html`（不是探险选择页）
- [ ] F12 Console 无红色错误

---

## iPad Safari 测试

- [ ] 答完最后一题 → 拍照页 → 点「查看通关成绩」→ 正确跳转通关页
- [ ] 撒花动画在 iOS 上正常播放
- [ ] 合影区横向可滑动（手指左右滑）
- [ ] 长按缩略图 → iOS 弹出系统菜单「存储到"照片"」

## iPad PWA 主屏幕图标测试

- [ ] 联网打开首页一次 → 设置页确认版本更新至「M25」
- [ ] 设置页底部显示「✅ 离线准备已完成」
- [ ] **飞行模式**下从图标启动 → 完整走一关 → 通关页正常加载（不报 response is null）
- [ ] 飞行模式下撒花、合影、返回首页全部正常

---

## 备注

- 撒花是纯 CSS emoji 动画，无依赖库，飞行模式下同样有效
- `victory.html` 已加入 SW 预缓存清单（CACHE_VERSION v2.3），联网开一次首页即可离线访问
