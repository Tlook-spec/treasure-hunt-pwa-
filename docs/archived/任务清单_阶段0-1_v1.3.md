# 寻宝游戏 PWA - 阶段 0-1 任务清单（v1.3 Windows 版）

> 这份是你接下来 **2-3 周** 的完整作战手册。
>
> **本版本基于 PRD v1.3 双站点架构 + Windows 11 + VS Code + Claude Code 优化**。
>
> **使用方法**：
> 1. 按顺序执行任务，前置未完成不要跳
> 2. 每个任务的【操作指令】直接复制给 Claude Code（VS Code 扩展或 CLI 都行）
> 3. 完成后用【验证清单】检查
> 4. 在任务前的 `[ ]` 打钩 `[x]` 表示完成

---

## 📋 阶段 0-1 任务总览

**阶段 0：环境搭建 + 试水**
- ✅ T01. 注册 GitHub 账号
- ✅ T02. 安装 VS Code 和 Claude Code
- ✅ T03. 创建项目文件夹和初始化
- ✅ T04. 完成第一个 Hello World PWA
- ✅ T05. 部署到 GitHub Pages
- ✅ T06. 在 iPad 上「添加到主屏幕」试用
- ✅ T07. 扫码功能试水（含 Eruda 调试工具）
- [ ] **T08. 架构调整：拆分为双站点结构**（v1.3 新增）

**阶段 1：产品设计**
- [ ] T09. 阅读 PRD v1.3
- [ ] T10. 用 Excalidraw 画线框图（按 v1.3 新口径）
- [ ] T11. 整理每个页面的数据清单
- [ ] T12. 选定视觉风格
- [ ] T13. 阶段 1 总结里程碑

---

## 💡 工作流约定

你接下来会同时用三个 Claude 相关工具，分工建议：

| 工具 | 主要用途 |
|------|---------|
| **claude.ai 网页版** | 讨论方案、问概念、调整计划（和我聊天的这个） |
| **VS Code 里的 Claude Code 扩展** | 主力开发——写代码、改代码、看文件 |
| **PowerShell 里的 Claude Code CLI** | 跑 git 命令、装包、长输出操作 |

**默认模型**：使用 **Sonnet 4.6** 处理日常开发；遇到难题或大型架构设计时切换到 **Opus 4.7**（输入 `/model` 切换）。

---

## ✅ 阶段 0：已完成回顾

### [x] T01-T03（已完成）

- GitHub 账号注册 + 邮箱验证
- VS Code 安装 + Claude Code CLI + VS Code 扩展
- 项目文件夹 `treasure-hunt/` 创建
- 三份文档放置（CLAUDE.md、docs/PRD.md、docs/任务清单）

### [x] T04. 第一个 Hello World PWA（已完成）

生成了：
- `index.html`：「🗺️ 寻宝游戏」标题 + 开始游戏按钮
- `manifest.json`：PWA 配置
- `service-worker.js`：离线缓存
- `README.md`：项目说明
- `docs/PROGRESS.md`：进度记录

本地预览通过（PowerShell + Python http.server / Node.js）。

### [x] T05. 部署到 GitHub Pages（已完成）

- GitHub 仓库 `treasure-hunt-pwa` 创建（Public）
- 通过 GitHub Desktop / Git 把代码 push 到 main 分支
- Settings → Pages 配置 main 分支根目录
- 访问网址正常打开

**注意**：T08 架构调整后，访问路径会变化（从根路径变到 `/play/`），届时需要刷新一次。

### [x] T06. iPad 添加到主屏幕（已完成）

- iPad Safari 打开 GitHub Pages 网址
- 通过分享按钮「添加到主屏幕」
- 飞行模式下能正常打开（验证 Service Worker 工作）

**注意**：T08 架构调整后，**旧图标指向的网址会变成根入口页**（不是游戏页）。届时需要删除旧图标，重新添加新的 `/play/` 网址。

### [x] T07. 扫码功能试水（已完成）

- 集成了 Eruda 调试控制台（带 `?debug=1` 启用）
- 创建了 `test-scan.html` 扫码测试页
- 使用 html5-qrcode 库
- 处理了摄像头权限边界情况
- 本地 + iPad 真机测试通过 ✅
- **重大里程碑达成**：技术可行性验证通过，PWA 方案完全可行 🎉

**T07 完成后的项目状态（架构调整前）**：

```
treasure-hunt/
├── index.html
├── manifest.json
├── service-worker.js
├── test-scan.html          # T07 扫码测试页
├── README.md
├── .gitignore
├── CLAUDE.md               # v1.3 已更新
└── docs/
    ├── PRD.md              # v1.3 已更新
    ├── PROGRESS.md
    ├── 版本路线图.md
    ├── Windows开发备忘.md
    └── 任务清单_阶段0-1_Windows版.md  # 本文档
```

---

## 🚧 阶段 0 尾声：架构调整

### [ ] T08. 架构调整：拆分为双站点结构

**前置**：T07 + 已经更新 CLAUDE.md 和 PRD.md 到 v1.3
**预计时间**：1-1.5 小时
**类型**：架构重构

**背景**：

v1.3 PRD 把项目从「单一 PWA」改为「双站点」：

```
treasure-hunt/
├── admin/      ← 编辑端（普通网页，不做 PWA）
├── play/       ← 游戏端（PWA，iPad 用）
└── shared/     ← 共享代码
```

你 T04-T07 做的所有文件都属于「游戏端」，需要移到 `play/` 子目录。`admin/` 和 `shared/` 暂时空着，MVP 阶段会填充。

**【复制给 Claude Code 的指令】**

```
你好 Claude Code。我们刚刚把 PRD 和 CLAUDE.md 升级到 v1.3，最重要的变化是：

项目从单一 PWA 改为双站点架构：
- admin/  编辑端（普通网页，不做 PWA）  
- play/   游戏端（PWA，iPad 用）  
- shared/ 共享代码

请先做这两件事：

1. 重新读项目根目录的 CLAUDE.md（v1.3）和 docs/PRD.md（v1.3），
   重点理解 §1 项目背景、§3 项目结构、§4 编辑端和游戏端的分工。
   读完简单跟我说说你的理解，特别是双站点的区别。

2. 等我说"OK，开始执行"，再做架构调整。
```

**等 Claude Code 确认理解后，发送下面的指令**：

```
OK，开始执行架构调整。请按以下步骤操作：

【第 1 步：创建新目录结构】

在 treasure-hunt/ 下创建三个新子目录：
- admin/    （编辑端代码，目前空着）
- play/     （游戏端代码）
- shared/   （共享模块，目前空着）

注意：CLAUDE.md、README.md、docs/ 保持在根目录。

【第 2 步：把 T04 和 T07 的文件移到 play/ 子目录】

把根目录下的以下文件移动到 play/：
- index.html
- manifest.json  
- service-worker.js
- test-scan.html
- 以及 T04 和 T07 生成的所有 CSS / JS / 图标资源文件夹

如果有专属的 styles/ 或 scripts/ 文件夹，也一起移动。

移动时注意：保持文件之间的相对路径引用正确。

【第 3 步：修改 service-worker.js 的缓存路径】

因为现在在 play/ 子目录下，service-worker.js 里的：
- 缓存文件路径（cache.addAll 里的路径）
- scope 路径

需要相应调整。重点是缓存路径要相对于 play/ 目录。

【第 4 步：修改 manifest.json】

manifest.json 里的 start_url 和 scope 需要调整：
- start_url: "/treasure-hunt/play/"（如果部署到 GitHub Pages 项目页）
- 或保持相对路径 "./"

请你判断最稳妥的方式（我的 GitHub Pages 是项目页，
网址是 https://用户名.github.io/treasure-hunt/）。

【第 5 步：创建根目录的简单入口页】

在 treasure-hunt/index.html（如果被移走了就新建）做一个简单的「主入口」：
- 显示项目名「寻宝游戏」+ 简短说明
- 两个大按钮链接：
  - 「📝 进入编辑端」→ ./admin/
  - 「🎮 进入游戏端」→ ./play/
- 简单的样式，居中显示，移动端也能看
- 不需要 PWA、不需要复杂逻辑，纯静态 HTML

这样访问 https://用户名.github.io/treasure-hunt/ 时不会 404。

【第 6 步：在 admin/ 目录创建占位 index.html】

admin/ 暂时空，但放一个占位 index.html：
- 显示「编辑端开发中...」
- 「← 返回主页」按钮链接到 ../
- 这样访问 /admin/ 路径不 404

【第 7 步：更新 README.md】

更新或创建根目录 README.md：
- 项目名 + 简介
- 项目结构说明（admin/、play/、shared/、docs/）
- 访问网址说明（两个端的网址）
- 本地预览方法（注意现在要访问 /play/ 路径）
- 开发文档链接（docs/PRD.md 等）

【第 8 步：更新 docs/PROGRESS.md】

记录：
- T08 架构调整完成
- 当前项目结构
- 已完成任务列表更新
- 下一步：T09 阅读 PRD v1.3

【第 9 步：本地验证】

帮我用 PowerShell 启动本地服务器，验证：
- http://localhost:8000/ 显示根入口页
- http://localhost:8000/play/ 显示原来的「寻宝游戏」+ 测试扫码入口
- http://localhost:8000/play/test-scan.html 还能扫码
- http://localhost:8000/admin/ 显示「编辑端开发中」占位页

【第 10 步：commit + push 到 GitHub】

commit message 写：
"架构调整 v1.3：拆分为 admin/play/shared 双站点结构"

push 后告诉我新的 GitHub Pages 网址路径。

【最后】

给我一个总结：
- 改了哪些文件？移动了哪些？
- 现在的目录结构是什么样？
- 我需要做什么验证？
- iPad 上添加到主屏幕的图标需要重新加吗？为什么？
```

**你完成后要做的验证**：

**本地测试**：

```powershell
cd C:\路径\treasure-hunt
python -m http.server 8000
```

然后浏览器访问以下网址，依次确认：

- [ ] `http://localhost:8000/` → 看到根入口页（两个链接）
- [ ] `http://localhost:8000/play/` → 看到原来的「寻宝游戏」页面
- [ ] `http://localhost:8000/play/test-scan.html` → 扫码功能还能用
- [ ] `http://localhost:8000/admin/` → 看到「编辑端开发中」占位
- [ ] F12 检查没有红色错误

**GitHub Pages 测试**：

- [ ] push 完成后等 1-5 分钟
- [ ] 浏览器访问 `https://用户名.github.io/treasure-hunt/` 看到根入口页
- [ ] 点击「进入游戏端」跳到 `/play/`，原来的 PWA 正常
- [ ] 测试扫码功能还能用

**iPad 操作**：

- [ ] 长按旧的「寻宝游戏」主屏幕图标 → 移除 App（删除旧的）
- [ ] iPad Safari 打开 `https://用户名.github.io/treasure-hunt/play/`
- [ ] 分享 → 添加到主屏幕（重新添加新的）
- [ ] 飞行模式下能正常打开（验证 Service Worker 在新路径下工作）

**遇到问题**：

- **如果 Service Worker 缓存出错（页面打不开/资源 404）**：把 F12 Console 错误截图发给 Claude Code，让它修复缓存路径
- **如果 iPad 上 PWA 不能离线**：在 iPad Safari 上用 `?debug=1` 打开新网址，看 Eruda Console 报错
- **如果 GitHub Pages 路径异常**：在 GitHub Settings → Pages 检查部署源分支

**验证清单**：

- [ ] 项目结构正确（admin/、play/、shared/、docs/）
- [ ] play/ 下能找到原来 T04 + T07 的所有文件
- [ ] 本地预览全部正常
- [ ] GitHub Pages 部署成功
- [ ] iPad 上重新添加到主屏幕、离线可用
- [ ] PROGRESS.md 已更新
- [ ] git commit + push 完成

---

## 🎯 阶段 1：产品设计

### [ ] T09. 阅读 PRD v1.3

**前置**：T08
**预计时间**：30-60 分钟阅读
**类型**：文档确认

**你要做的事**：

1. 在 VS Code 里打开 `docs/PRD.md`
2. 用 Markdown 预览模式完整读一遍（快捷键 `Ctrl + Shift + V`）
3. **重点关注 v1.3 相比 v1.2 的变化**（见 §10 文档更新记录的 v1.3 条目）
4. 划重点：
   - §1.3 双站点架构 + 三层结构 L1/L2/L3
   - §3.2 MVP 范围（已收敛）
   - §4.1 编辑端详细
   - §4.2 游戏端详细（注意 L2 多题连答 + 通关合影机制）
   - §6 数据模型（短 ID、6 位数字码、usedCount）

**【可选：让 Claude Code 帮你 review】**

```
请读一下 docs/PRD.md（v1.6），告诉我：
1. 有没有逻辑矛盾的地方
2. v1.3 的双站点架构和三层结构是否清晰
3. MVP 范围划分是否合理
4. 还有哪些技术实现上的潜在坑

只指出问题，不要直接改文件。我们讨论后再决定是否修改。
```

**验证清单**：

- [ ] 完整读过 PRD v1.3
- [ ] 理解双站点（admin/play）的分工
- [ ] 理解三层结构（L1=探险，L2=点位多题，L3=题目）
- [ ] 知道每个 L2 通关时拍一张合影的设计
- [ ] 记下了 1-3 个你最关注的功能或疑问

---

### [ ] T10. 画核心页面线框图

**前置**：T09
**预计时间**：6-10 小时（分散到 4-5 天通勤完成）
**类型**：设计任务（你独立完成，不需要 Claude Code）

**你要做的事**：

1. iPad 上 App Store 搜 Excalidraw，免费安装（或者用网页版 excalidraw.com）
2. 创建新文件，命名「寻宝游戏线框图」
3. 按下面的清单依次画

**v1.3 线框图清单（24 张，按双站点 + 三层结构）**：

**【编辑端 admin/】**（10 张）

1. 编辑端首页 / 导航（左侧菜单 + 主区域）
2. L1 探险列表（卡片列表 + 「新建探险」按钮）
3. 新建 / 编辑 L1 表单（探险名、简介、推荐人数、密码等）
4. L1 详情页（含 L2 点位列表 + 排序 + 「打印二维码」按钮）
5. 编辑 L2 表单（含题目绑定区 + 拖拽题目顺序）
6. 题库管理主页（筛选条 + 题目列表 + usedCount 显示）
7. 新建 / 编辑题目表单（题干、选项、答案、标签等）
8. CSV 批量导入流程页（上传 → 预览 → 确认 → 结果）
9. JSON 导入导出页（导入时的「检测到未完成游戏」确认）
10. 删除确认弹窗 + 5 秒 Undo toast

**【游戏端 play/】**（14 张）

11. 启动页（开始游戏 + 设置入口）
12. 设置 / 导入数据页（含「导入 JSON」按钮）
13. 探险选择页（卡片列表）
14. 开始冒险页（探险名 + 大按钮）
15. L2 提示展示页（第 N 站 + 提示文本 + 扫码按钮）
16. 扫码界面（取景框 + 「输入数字码」+ 「找不到？」按钮）
17. 6 位数字码输入弹窗
18. 「找不到？」L1 密码输入弹窗
19. 扫错码友好提示
20. 答题页面（题目 + 选项 + 求助按钮 + 「第 N / M 题」进度）
21. 答错 1 次的温柔提示（再想想）
22. 答错 2 次显示提示
23. 答错 3 次保底通过页
24. L2 通关拍合影页（拍照按钮 + 跳过按钮）
25. 下一站提示页
26. 极简通关页（撒花 + 总分 + 用时 + L2 合影集 + 返回首页）

**MVP 单人版不画的（推到 V1）**：

- 人数选择
- 玩家创建（头像）
- 队名设置
- 答题归属选择
- 投票环节
- 颁奖典礼

**画图要求**：

- 每张用一个大方框代表屏幕
- 编辑端用横向方框（桌面），游戏端用竖向方框（iPad 竖屏）
- 里面用小方框代表元素，标注文字
- 元素之间用箭头标注跳转关系
- 不要追求美观，「看得懂」就行

**画到一半或全部完成时**，可以截图发给 claude.ai 网页版让我帮你 review。

**验证清单**：

- [ ] 至少画完 26 张核心线框图
- [ ] 编辑端和游戏端**清晰分开**
- [ ] L2 内多题连答的流程清晰
- [ ] 每个 L2 通关合影的位置清楚
- [ ] 跳转关系（箭头）画清楚
- [ ] 导出为 PNG 或 PDF 保存到 `docs/wireframes/` 文件夹

---

### [ ] T11. 整理每个页面的数据清单

**前置**：T10 至少完成 50%
**预计时间**：2-3 小时
**类型**：设计任务

**你要做的事**：

在 `docs/` 下创建一个新文件 `pages-data.md`，按页面列出每个页面需要的数据和操作。

**模板示例**：

```markdown
# 各页面数据清单

## 编辑端 admin/

### 1. 编辑端首页
**显示**：
- App 名称、导航菜单（探险/题库/导入导出/设置）

**操作**：
- 点击导航 → 跳到对应页

**需要的数据**：无（静态导航）

---

### 2. L1 探险列表
**显示**：
- 所有 L1 探险的卡片列表
  - 每张卡片：探险名、L2 点位数、上次编辑时间

**操作**：
- 点击卡片 → 进入 L1 详情页
- 「新建探险」按钮 → 弹出新建表单
- 长按卡片 → 删除选项

**需要的数据**：
- 所有 Level 列表（从 IndexedDB 读取）

---

## 游戏端 play/

### 13. 探险选择页
**显示**：
- 所有探险的卡片列表
  - 每张：探险名、L2 点位数、上次游玩

**操作**：
- 点击卡片 → 跳转开始冒险页（携带 levelId）

**需要的数据**：
- 所有 Level 列表（从 IndexedDB 读取）
- 当前游戏会话状态（判断是否有未完成游戏）

---

[继续每个页面...]
```

**【可选：让 Claude Code 帮你起草初稿】**

```
请基于 docs/PRD.md（v1.3）和 docs/wireframes/ 里的线框图，
帮我起草 docs/pages-data.md 的初版。

为每个页面（admin/ 和 play/ 都要）列出：
- 显示元素
- 操作和跳转
- 需要的数据（哪些字段、从哪里来）

不要太详细，每个页面 5-10 行就够。完成后我会自己 review 和补充。
```

**验证清单**：

- [ ] 编辑端每个页面都有数据清单
- [ ] 游戏端每个页面都有数据清单
- [ ] 跨页面的数据传递清楚（如 levelId 怎么传递）
- [ ] 文件保存到 `docs/pages-data.md`

---

### [ ] T12. 选定视觉风格

**前置**：T11
**预计时间**：1-2 小时
**类型**：设计决策

**你要做的事**：

在 `docs/` 下创建 `design-tokens.md`，记录所有视觉决策。

**做的决策**：

1. **配色方案**：
   - 去 https://coolors.co/ 或 https://colorhunt.co/ 找一组儿童友好的配色
   - 记录主色、辅助色、强调色、错误提示色、背景色的色号

2. **字体**：
   - 中文：系统默认无衬线（Windows 微软雅黑 / iOS 苹方）
   - 英文：系统默认

3. **字号层级**：
   - 大标题、副标题、正文、提示文字的 px 值

4. **圆角**：
   - 按钮、卡片、图片的圆角值

5. **间距**：
   - 内边距、元素间距的基础值

6. **整体风格关键词**（选 1-2 个）：
   - 圆润 / 卡通 / 明亮 / 简约 / 清新 / 绘本风

**编辑端 vs 游戏端**：

- 编辑端可以稍微「专业」一些（毕竟家长用）
- 游戏端要「儿童友好」（孩子用，按钮大、色彩明亮、动画活泼）
- 两端共用相同的色系，但风格略有差异

**示例 design-tokens.md**：

```markdown
# 视觉设计规范

## 配色

### 主色
- 主色：#4A90E2（明亮蓝）
- 主色深：#3A7BC8
- 主色浅：#7BB0EE

### 辅助色
- 辅助色：#F5A623（暖橙）
- 强调色：#7ED321（鲜绿，用于"成功"）
- 错误色：#FF9F89（柔和粉橙，不刺眼）

### 中性色
- 背景色：#FFFFFF
- 卡片背景：#F8F9FA
- 文字主色：#2C3E50
- 文字次色：#6C757D
- 边框色：#E9ECEF

## 字号
- 大标题：32px
- 标题：24px
- 副标题：18px
- 正文：16px
- 提示：14px
- 按钮：18px

## 圆角
- 按钮：12px
- 卡片：16px
- 图片：8px

## 间距
- 基础间距：8px
- 标准间距：16px
- 大间距：24px
- 超大间距：32px

## 阴影
- 卡片阴影：0 2px 8px rgba(0,0,0,0.1)
- 浮起阴影：0 4px 12px rgba(0,0,0,0.15)

## 风格关键词
- 编辑端：简约、清新、专业
- 游戏端：圆润、明亮、绘本风、有童趣
```

**完成后**：把这个文件的内容也补充到 `CLAUDE.md` 的 §9 章节。

**验证清单**：

- [ ] 配色组确定且写入 design-tokens.md
- [ ] 字号、圆角、间距规范明确
- [ ] 编辑端 vs 游戏端的风格差异说清楚
- [ ] CLAUDE.md §9 已更新

---

### [ ] T13. 阶段 0-1 总结里程碑

**前置**：T12
**预计时间**：30 分钟
**类型**：里程碑确认

**【复制给 Claude Code 的指令】**

```
我们完成了阶段 0 和阶段 1 的所有任务（T01-T12），请帮我做一次完整检查和总结：

1. 检查项目结构是否完整：
   - 根目录：CLAUDE.md（v1.3）、README.md、index.html
   - admin/：占位 index.html
   - play/：完整的 PWA（index.html、manifest.json、service-worker.js、test-scan.html 等）
   - shared/：空（MVP 阶段填充）
   - docs/：PRD.md（v1.3）、PROGRESS.md、版本路线图.md、Windows开发备忘.md、
            wireframes/（含 26 张线框图）、pages-data.md、design-tokens.md、本任务清单

2. 验证 PWA 还能正常工作：
   - 提醒我去 GitHub Pages 网址打开测试
   - 提醒我在 iPad 上从主屏幕图标打开测试

3. 写一份阶段 0-1 完成报告，放在 docs/MILESTONE_0-1.md，包含：
   - 完成的任务清单（T01-T12）
   - 当前项目能做什么
   - 下一阶段（阶段 2 - MVP 开发）即将开始
   - 给我一些鼓励的话 😊

4. 更新 docs/PROGRESS.md，标记阶段 0-1 完成

5. 帮我做一次完整 commit + push，commit message:
   "完成阶段 0-1：环境搭建 + 双站点架构 + 产品设计"
```

**最后给自己**：

🎉 **庆祝时刻！**

走到这里你已经：

- ✅ 拥有完整的 Windows 开发环境
- ✅ 部署过 PWA 到 GitHub Pages
- ✅ 在真 iPad 上跑过自己做的东西
- ✅ 验证过扫码这个核心功能
- ✅ 学会用 Eruda 调试 iPad 网页
- ✅ 完成了双站点架构调整
- ✅ 完成了产品的全面设计
- ✅ 跨过了 70% 想做 App 的人会放弃的关卡

给自己买个奖励吧——一杯好咖啡、一顿好饭、或者只是和孩子说「爸爸/妈妈在做一个给你玩的游戏哦」。

---

## 📌 完成阶段 0-1 后，下一步

完成本任务清单后，告诉 claude.ai 网页版：「我完成了阶段 0-1，需要 MVP 开发的任务清单」，我会基于你的实际进度，给你 MVP 阶段的详细任务清单（5-6 周内容）。

**MVP 大致路径**（预告，详见后续任务清单）：

- MVP 阶段 1：搭骨架（admin 和 play 各自的项目结构、shared 模块、IndexedDB）
- MVP 阶段 2：编辑端核心（L1/L2/L3 CRUD、CSV 导入、二维码生成、JSON 导出）
- MVP 阶段 3：游戏端核心（扫码主流程、L2 多题连答、答题逻辑、求助、拍照）
- MVP 阶段 4：联调（JSON 同步、暂停续玩、应急通关）
- MVP 阶段 5：真实测试 + 修 bug

---

## 💡 一些跨任务的提醒

### 关于 git commit 节奏

每完成一个任务（T08、T09……），让 Claude Code 帮你 commit + push 一次：

```
请帮我用 GitHub Desktop / git commit + push 当前修改，
commit message 写：完成 T0X：[任务名]
```

这样以后随时能回到任何任务完成时的状态。

### 关于「卡住超过 30 分钟」

如果某个任务卡了 30 分钟还没解决，**不要硬扛**：

1. 把当前情况写清楚发给 claude.ai 网页版（我）
2. 包括：你想做什么、你做了什么、Claude Code 说了什么、报了什么错
3. 网页版 Claude 视角更全面，能从更高维度帮你诊断

### 关于切换模型

- **默认 Sonnet 4.6**：日常写代码、改 bug
- **遇到难题切 Opus 4.7**：在 Claude Code 里输入 `/model` 切换
- 解决后切回 Sonnet 节省额度

### 关于「想跳过某个任务」

任务之间是有依赖的，但偶尔可以灵活调整：

- ✅ T10（画线框图）可以和 T09（阅读 PRD）并行
- ✅ T11、T12 可以同时推进
- ❌ T08（架构调整）不能跳过，是 MVP 开发的基础
- ❌ T09（阅读 PRD）不能跳过，避免 MVP 开发时方向跑偏

---

## 🔗 相关文档

- `CLAUDE.md`（v1.3）- 项目说明书，Claude Code 自动读取
- `docs/PRD.md`（v1.3）- 完整产品需求
- `docs/版本路线图.md`（v1.1）- MVP / V1 / V2 时间估算
- `docs/Windows开发备忘.md` - Windows 环境技巧速查
- `docs/PROGRESS.md` - 实时进度记录（Claude Code 自动维护）

---

**祝你接下来 2-3 周顺利！我们 MVP 见。** 🚀
