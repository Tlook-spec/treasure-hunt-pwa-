# 功能名：V1-11 编辑端 L2 三段文案（答题引导 + 通关庆祝）

> 改动范围：`admin/pages/levels.html`（新增两个 textarea）、`admin/scripts/point-form.js`（读写新字段）

---

## 桌面浏览器测试（Windows + Chrome）

### 表单显示

- [ ] 打开任意点位编辑表单 → 出现「🎉 孩子看到的文案（都可留空）」分组
- [ ] 分组开头显示出现顺序说明：「发现文案 → （地图动画）→ 答题引导 → 答题 → 通关庆祝 → （盖章动画）」
- [ ] 显示「答题引导」输入框，附带小字说明（留空就不弹）
- [ ] 显示「通关庆祝」输入框，附带小字说明（留空就不弹）

### 保存与回读

- [ ] 两个框都填写内容 → 点「保存点位」→ 重新打开同一点位 → 两个值都正确回显
- [ ] 只填答题引导，通关庆祝留空 → 保存不报错 → 重开只有答题引导有内容
- [ ] 只填通关庆祝，答题引导留空 → 保存不报错 → 重开只有通关庆祝有内容
- [ ] 两个框都留空 → 保存不报错 → 重开两个框都为空

### 老数据兼容

- [ ] MVP 阶段建的旧 L2（从未填过这两个字段）→ 打开编辑表单不报错 → 两个框显示为空

### IndexedDB 验证

- [ ] DevTools → Application → IndexedDB → treasure-hunt-admin-db → points → 点开某条记录
- [ ] 能看到 `questionIntroText` 和 `completionText` 字段，值为填入的内容或空串

### F12 检查

- [ ] 整个操作过程 Console 无红色错误

---

## iPad Safari 测试

- [ ] 在 iPad Safari 上打开编辑端，进入任意点位编辑 → 两个新输入框正常显示
- [ ] 填写内容并保存 → 重开值还在
- [ ] Eruda Console（?debug=1）无红色错误
