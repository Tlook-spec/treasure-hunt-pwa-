# 功能名：V1-29 游戏端多人版通关庆典 + 小组颁奖

> 通关页（`victory.html`）新增「队伍展示 + 小组颁奖」，**仅多人**（`session.players` 非空）显示，单人维持现状。
> 读 `level.groupAwardName / groupAwardImage`，没配走内置默认（默认奖名「通关纪念奖」+ 默认奖杯 🏆）。
> **不动**结算幂等 / 撒花 / 成绩单错题本 / 海报生成；读取全程容错。

---

## 桌面浏览器测试（Windows + Chrome）

### 多人 + L1 配了奖名奖图
- [ ] 用多人会话打通一局（开局选 ≥2 人，填队名/编号/颜色，部分拍头像）
- [ ] L1 在编辑端配了 groupAwardName + groupAwardImage（V1-28），导出导入到游戏端
- [ ] 通关页靠上显示「🚩 队名」+ 一排全体头像小圆
- [ ] 撒花之后（约 0.5s）奖区从小放大弹出：奖图 = 配置的图，文字「🎉 队名 获得【奖名】！」
- [ ] 奖图下方再有一排全体头像
- [ ] F12 Console 无红色错误

### 多人 + L1 没配奖
- [ ] 换一个没配 groupAwardName/groupAwardImage 的 L1，多人通关
- [ ] 奖名走默认「通关纪念奖」，奖图走默认奖杯 🏆，**不报错、不空白**

### 多人但有人没拍头像
- [ ] 队里有人没拍头像 → 该人头像位显「颜色块（player.color）+ 编号」圆
- [ ] 拍了头像的人显头像图；两种混排正常

### 单人维持现状（关键回归）
- [ ] 单人打一局（开局人数 1）→ 通关页**完全和以前一样**，无队伍区、无颁奖区
- [ ] F12 查 session：`players` 为 `[]`（或老存档无此字段）→ 不显示多人区

### 结算幂等没被破坏
- [ ] 通关页刷新一次 → 用时**不变**（endedAt 只写一次）、总分/答对数不变
- [ ] 刷新后颁奖动画再演一次 → 可接受（不报错即可）

### 不破坏既有功能
- [ ] 总分 / 答对题数 / 用时 三格正常
- [ ] 合影区、按站成绩 + 错题本正常
- [ ] 生成成就海报正常（海报本任务**不含**队伍信息，维持现状）

---

## iPad Safari 测试

- [ ] 多人通关：队名 + 头像一排在 iPad 宽度下不溢出/不错位
- [ ] 颁奖弹出动画流畅（scale + 渐显）
- [ ] 奖图（真实上传图）清晰、不变形
- [ ] 单人通关页与以前一致

---

## iPad PWA 主屏幕图标测试（三态）

### 有网 Safari 直接访问
- [ ] 同上 iPad Safari 测试全通

### PWA 有网（主屏幕图标）
- [ ] 缓存更新后（设置页 BUILD_TAG 显示 V1-29），多人通关 → 队伍区 + 颁奖正常

### PWA 飞行模式（先联网缓存过 v2.29）
- [ ] 飞行模式下多人通关 → 队伍展示 + 颁奖正常（头像从本地 photos 表读，离线可用）
- [ ] 单人通关页正常
- [ ] Eruda 无红色错误

---

> **说明**：颁的是【整队一枚奖】，不区分个人（个人奖在 V2）。海报里暂不含队伍信息（避免改 Canvas 引入风险）。
