# 寻宝游戏 PWA - 任务清单(V1,v1.9)

> 这是 MVP 之后的 V1 作战手册。**对应 PRD v1.9 + CLAUDE.md v1.9**。
>
> **V1 主题**:先把单人体验做香(命中真机反馈)→ 再做探险地图+故事+海报(沉浸感)→ 收尾杂项 → 最后才考虑多人(最不确定,玩过前面再定)。
>
> **编码规则**(同 MVP 的 T/M 流水码):全 V1 用 **V1-01、V1-02 … 一路连续往下排**,分区(A/B/C/D/E)只是标题,不重新计数。
>
> **使用方法**(同 MVP):
> 1. 严格按顺序做,前置没完成不要跳(MVP 串行推进原则继续生效)
> 2. 每个任务的【复制给 Claude Code 的指令】整段复制进 Claude Code
> 3. 完成后用【验证清单】自查,在 `[ ]` 里打钩 `[x]`
> 4. 每做完 1 个任务 → 单行英文 git commit + push → 更新 PROGRESS.md
> 5. 游戏端每块做完必测 **iPad 三态**(Safari / PWA 有网 / PWA 飞行模式)
>
> **模型**:日常用 Sonnet;遇到难题或多文件大改动,`/model` 切 Opus,解决后切回。

---

## 📋 V1 总览(五个区，流水码 V1-01 ~ V1-35)

| 区 | 编码 | 内容 | 大小 | 状态 |
|------|------|------|------|------|
| **A 单人打磨** | V1-01 ~ V1-08 | 解析即时显示、通关成绩单&错题本(按站汇总)、扫码音效+动画、通关音效、拼音可切换、修 2 个已知 bug、出题用字指南、联调验收 | 小-中 | ✅ 完成 |
| **B 地图 + 故事 + 海报** | V1-09 ~ V1-20 | 地图上传+标记编辑器、L2 三段文案、L1 开场/结局故事、扫码后地图动画、盖章动画、地图三态、成就海报(一次到位含地图)、联调验收 | 中-大 | ✅ 完成 |
| **C 收尾杂项 + 题型** | V1-21 ~ V1-26 | 主动暂停按钮 ✅、~~题目插图~~(暂缓)、单 L1 追加导入/导出 ✅、填空题 ✅、随机抽题 ✅、联调验收 | 小-中 | 🔨 进行中(V1-21~25 已完成并发版给孩子,剩 V1-26 联调) |
| **D 多人(合作式轻量版)** | V1-27 ~ V1-31 | 开局玩家创建(头像可选+名字+编号+颜色)+队名、L1 小组奖配置、多人版庆典+小组颁奖、数据/续玩/导入导出兼容、联调验收 | 中 | ⬜ 已展开详细任务 |
| **E 真机测试 + 修 bug** | V1-32 ~ V1-35 | 带孩子户外验收、收尾打磨、里程碑、定 V2 | — | ⬜ 待开始 |

> **进度**:A / B / C / D 区均已展开为可复制的详细任务(A、B 已完成,C 进行中,D 待开发)。
>
> **题型工作已并入 C 区**(2026-06-23 调整):判断题(MVP 已做)+ 填空题(V1-24 做完)已覆盖需求,标签随机抽题(V1-25)也提前到 C 区。
>
> **D 区已大幅简化为「合作式轻量多人」**(2026-06-24 与用户确认):原「个人积分归属 / 难度加成 / 连续答对计数 / 答题之星 / 投票颁奖 / 保底奖」**全部推到 V2**。V1 多人只做:开局创建玩家(增加仪式感+合影归属)+ 答题流程不变(全队共享一个总分)+ 通关给整队颁一枚奖(每个 L1 自配奖名奖图)。D 区从 8 个任务瘦到 5 个,E 区顺延,全表收口 V1-01~V1-35。

---

# 🟦 A 区：单人打磨(V1-01 ~ V1-08，详细任务)

> 这一整块都建立在 MVP 已有代码上,**不重写,只增量加功能**。涉及文件大多是游戏端 `play/`,少量编辑端 `admin/` 和共享层 `shared/`。

---

## [ √] V1-01. 游戏端展示解析(explanation)

**前置**:无(A 区第一个任务)
**预计时间**:0.5-1 天(纯游戏端,基本只改一个文件)
**类型**:游戏端

> 📌 **实测更正**:`explanation`(解析)字段、编辑端"提示与解析"表单、CSV 的「解析」列**MVP 阶段就已经做好了**,数据也已经在题目对象上,而且**已经跟着 1.0 的 JSON 一起导入导出**(`exportFull` 倒整个题目对象、`importFull` 整个写回)。**唯一缺的是:游戏端从没把解析显示出来**(全局搜 `explanation` 在 `play/` 里一个都没有)。所以这个任务**不动数据模型、不动 schema、不动编辑端、不动 CSV**,只改 `play/pages/quiz.html`。

**【复制给 Claude Code 的指令】**

```
题目有 explanation(解析)字段,编辑端能填、CSV 和导入导出都带着,但游戏端答题时从没显示。
我只想在【保底通过】(bust:同一题错满 3 次、直接给正确答案、计 0 分)这一种情况下显示解析,
其余情况(自己答对、用求助答对)都维持现状直接下一题。只改 play/pages/quiz.html,
只动 showFeedback() 的 bust 分支,别碰 correct 和 wrong。

【动手前先确认】
先在 loadAndShowQuizScreen 拿到 question 后 console.log 一下,确认对象里有 explanation 字段
(loadCurrentQuizData 可能挑字段)。若没有,先告诉我,别硬写。

1. 改 showFeedback() 的 bust 分支:在"没关系,下次记住啦!🌟"这行下面、
   原有「好的,记住了!」确认按钮【上面】,依次插入:
   - 一行「正确答案：X」,X 用调用处传进来的 correctText。
     【不要】去抓 option-bust-correct 的文字——那是整个按钮,会把 A/B/C/D 标签
     和选项文字黏成"A选项文字"。
   - 若 explanation 非空,再加一行 <p class="feedback-hint-text">📖 解析：…</p>。
     explanation 为空就不加这行(别出现空的「📖」)。

2. correct 分支(答对)和 wrong 分支(答错重试)保持原样,完全不动——
   自己答对/用求助答对都直接下一题,答错只给提示。这是设计意图。

3. 调用处(第 258 行那次 showFeedback('bust', {...}))补传参数:
   showFeedback('bust', {
     correctText: question.options[ci],   // ci 已在上面第 254 行算出,直接用
     explanation: question.explanation,
     onConfirm,                            // 原有的别删
   })

4. 解析行复用 feedback-hint-text 样式(次要色、略小字号),别盖过确认按钮。

5. 写测试清单 docs/test-checklists/V1-01-展示解析.md,按 CLAUDE.md §5.7 模板的
   三段结构(桌面 Chrome / iPad Safari / iPad PWA 三态),覆盖:
   - 同一题故意错满 3 次保底【该题填了解析】→ 显示「正确答案:X」+ 解析,
     正确答案文字正确(无 A/B 标签黏连)
   - 同一题错满 3 次保底【该题没填解析】→ 只显示正确答案,不显示空的「📖」行
   - 自己一次答对 → 不显示解析,正常直接下一题
   - 用求助后答对 → 不显示解析,正常直接下一题
   - 答错(第 1-2 次重试)→ 仍只显示提示
   - F12 / iPad Eruda 无红色错误;iPad 三态全过

完成后:单行英文 git commit + push;更新 PROGRESS.md(V1-01 完成)。
```

**验证清单**:
- [ ] 保底通过显示正确答案 + 解析
- [ ] 答错重试仍只显示提示(不剧透解析)
- [ ] 测试 checklist 归档 + PROGRESS 更新

---

## [√] V1-02. 通关成绩单 + 错题本(按站汇总，全部通关后才显示)

**前置**:V1-01
**预计时间**:2-3 天(要顺带记录「选错的答案」)
**类型**:游戏端(通关页增强,错题本主体)

> 📌 **设计调整**:孩子玩时不会停下来逐站看错题,所以**取消每站中途回顾**,把所有回顾**集中到全部通关后的通关页**按站汇总。即时学习靠 V1-01(答对/保底当场显示解析)兜着,通关页这份是和家长一起复盘用的。

**【复制给 Claude Code 的指令】**

```
通关庆典页(victory.html)加「成绩单 + 错题本」,全部通关后按站显示。
动三个文件:play/pages/quiz.html、play/scripts/quiz-logic.js、play/pages/victory.html。
分两部分,先做完第 1 部分验证有数据,再做第 2 部分。

═══ 第 1 部分:答题时记录「选错的答案」(纯 session 数据,不进导出 JSON、不碰 schema)═══

A. play/scripts/quiz-logic.js —— recordQuestionResult 加第 6 个参数:
   - 函数签名加 wrongChoices,写入的对象改成:
     { questionId, answerAttempts, usedHelp, score, wrongChoices }

B. play/pages/quiz.html —— 收集并传参:
   1. 在 attemptCount 那组状态变量旁(约 105 行)加:let wrongChoices = [];
      在每题重置处(约 158-160 行,attemptCount=0 那块)一并:wrongChoices = [];
   2. 在 onSelectOption 的 else(答错)分支顶部,push 这次选错的字母:
      wrongChoices.push('ABCD'[selectedIndex]);
      —— 这样第 1-2 次重试和第 3 次保底都会记进去(它们都走 else),最多 3 个。
   3. 两处 recordQuestionResult 调用都把 wrongChoices 传进去:
      - 答对那次(约 223 行)
      - 保底那次(约 257 行)
   4. 读取时容错:别处读用 (qr.wrongChoices || []),老存档没这字段不报错。

✅ 第 1 部分做完先停:打一局故意错几题 → console.log(session.pointRecords)
   确认每条 questionResult 里有 wrongChoices 且有值,再做第 2 部分。

═══ 第 2 部分:victory.html 展示(在现有 总分/用时/合影 区下面加)═══

- 遍历 session.pointRecords,每站一张卡。
  站名要查 points 表:复用现成的 db.points.where('levelId').equals(session.levelId).toArray(),
  用 pointId 配出点位名。卡片标题:「第 N 站 · 点位名」。
- 统一口径(重要):答对 = 最终得分 > 0(一次对/错后对/求助都算答对);
  错题 = 最终得分 === 0(没答出来、保底)。
- 两级折叠展示:
  【一级·站卡】全部默认折叠。折叠副行显示「X 分 · 答对 X/Y 道题 · 点击查看详情吧」,
    展开后副行去掉「点击查看详情吧」。
    站点得分 = 该站 questionResults 的 score 之和;答对数 X = 该站 score>0 的题数。
    若该站 score>0 题数 = 总题数,展开顶部加一句「这站全对,厉害!🌟」。
  【一级展开后】逐题列出该站【所有题目】(不只错题),每题一行:题干 + 该题得分。
    得分为 0(错题)的题,得分数字标红、整行淡红底。
  【二级·每题】点击题目行可再展开,展示:
      - (仅 score===0)标一句「这题没答出来,直接看了答案」
      - 孩子选错的答案:从 wrongChoices 显示「字母+选项内容」;一次答对/旧存档无记录则不显示该行
      - 正确答案(字母 question.correctAnswer + 对应 options 内容)
      - (若 explanation 非空)解析;为空不显示空行
    得分为 0 的题【默认展开】,其余题默认折叠。
- 纯展示,不写任何 DB:别碰结算幂等(status/endedAt 只在约 103-106 行写一次)和撒花动画。
- 「按站算分」抽成一个纯函数(不写 DB,入参 pointRecords + 题目/点位数据,出参每站汇总),
  放 quiz-logic.js 或新建 shared/scripts/score-summary.js,B 区成就海报(V1-18)直接复用。
  保持简单,一个小函数就够,别搭新架子。

═══ 测试清单 docs/test-checklists/V1-02-通关成绩单错题本.md(按 §5.7 三段结构)═══
- 打一局:多站故意错几题,其中【一道错满 3 次保底(0 分)】+【一道错 2 次但第 3 次自己答对(5 分)】+ 一站全对
  → 每站卡:折叠副行得分/答对数(score>0)对;展开后逐题列出【所有题目+各自得分】
  → 0 分题:得分标红、整行淡红底、二级默认展开并标「直接看了答案」
  → 5 分(错后对)那道:正常色、二级默认折叠,展开后能看到选错答案 ← 重点验这条
  → 全对那站:展开顶部有「全对」鼓励,仍逐题列出(每题默认折叠)
- 没填解析的题 → 二级展开不显示空解析行
- 刷新通关页 → 成绩单还在、用时不变(验结算幂等)
- 老存档(旧 session 没 wrongChoices)进通关页 → 不报错,错题处显示降级文案
- F12 / iPad Eruda 无红色错误;iPad 三态全过

═══ 完成后(按顺序)═══
1. 更新 shared/version.js 的 BUILD_TAG 为 'V1-02-错题本 · <今天日期>'
   日期用 PowerShell Get-Date -Format 'yyyy-MM-dd' 取今天;任务号是 V1-02(不是 V1B-02)。
2. 更新 PROGRESS.md(V1-02 完成)
3. 单行英文 git commit + push,把 quiz.html / quiz-logic.js / victory.html /
   version.js / PROGRESS.md 一起提交。
```

**验证清单**:
- [ ] 答题时记录了选错的答案(wrongChoices,最多 3 个),不影响导出/schema
- [ ] 通关页按站显示:得分 / 答对数(score>0) / 共几题
- [ ] 站卡展开后逐题列出所有题目+得分;0 分题标红
- [ ] 每题二级展开含选错答案、正确答案、(有则)解析;0 分题默认展开
- [ ] 保底题有「直接看了答案」标注;全对站有「全对」鼓励
- [ ] 不破坏结算幂等 / 撒花;刷新用时不变
- [ ] 「按站算分」抽成可复用函数(供 V1-18 海报用)
- [ ] 测试 checklist + PROGRESS 更新

---

## [√] V1-03. 扫码成功音效 + 动画

**前置**:无
**预计时间**:0.5-1 天
**类型**:游戏端(仪式感)

> 📌 **实测更正**:`scan.html` 里**已经在调** `new Audio('../assets/sounds/ding.mp3').play()`(两处),但 **`ding.mp3` 这个文件在仓库里根本不存在**,也没进 Service Worker 预缓存,所以现在扫码其实没声音(失败被 `.catch()` 悄悄吞了)。答题音效 `correct.mp3`/`wrong.mp3` 已经有了且接好了,不用动。
>
> 📌 **决定**:音效**默认就响,不做 App 内开关**——想安静直接用 iPad 音量键/静音拨片即可,少一个状态少一处测试。
>
> ⚠️ 你需要准备 1 个**短**「叮咚/扫中」音效(< 100KB,mp3),命名 `ding.mp3`,放到 `play/assets/sounds/`。免费来源:mixkit.co、freesound.org。

**【复制给 Claude Code 的指令】**

```
扫码成功音效 + 动画。ding.mp3 我已放进 play/assets/sounds/(还没提交,commit 时记得 git add)。
动 play/service-worker.js + play/pages/scan.html。

0. 先确认 play/assets/sounds/ding.mp3 在本地真实存在,再往下做。

1. service-worker.js:
   - 把 './assets/sounds/ding.mp3' 加进 PRECACHE_URLS(已列 correct.mp3/wrong.mp3 那个数组)。
   - CACHE_VERSION 当前是字符串 'treasure-hunt-runtime-v2.5',版本号 +1 → 'treasure-hunt-runtime-v2.6'。

2. scan.html 音频解锁(关键):
   - 没有共享 unlock 函数:照搬 quiz.html 第 136-139 行那段内联解锁写法(muted 静默播一下再复位)。
   - 顶部建常驻 const dingSound = new Audio('../assets/sounds/ding.mp3');成功时 dingSound.currentTime=0;
     dingSound.play() 复用它,别每次 new Audio()。
   - 解锁要在用户手势里做,且【两个入口都做】:
     * btn-start-scan(点击开始扫码,约 415 行)
     * btn-manual-input(⌨️ 手动输码按钮,约 421 行)
     因为孩子可能不开摄像头直接手动输码,且手动输满 6 位是 setTimeout 自动提交(约 517 行)不在手势里,
     必须在更早的手势就解锁。
   - 确认两处播放都生效:摄像头成功(约 374 行)、手动输码成功(约 536 行)。

3. 扫码成功动画:取景框变绿 + ✅ 放大淡出(或 CSS 小星星),约 0.8 秒后跳 quiz.html。别拖、别卡。

4. 音效默认开,不加 App 内开关,音量交给 iPad 系统。

5. 测试 checklist docs/test-checklists/V1-03-扫码音效.md(按 §5.7 三段结构):
   - 摄像头扫码成功 → 叮咚 + 动画 → 进答题
   - 手动输码成功 → 同样有声(验两处调用)
   - 【不开摄像头、直接手动输码】也有声(验 manual 入口解锁)
   - iPad PWA 有声;飞行模式(先联网缓存过 v2.6)仍有声
   - iPad 三态

完成后(按顺序):
1. shared/version.js 的 BUILD_TAG 改 'V1-03-扫码音效 · <今天日期>'(PowerShell Get-Date 取日期;任务号 V1-03)
2. 更新 PROGRESS.md(V1-03 完成)
3. 单行英文 commit + push,把 ding.mp3 / service-worker.js / scan.html / version.js / PROGRESS.md 都加进去。
```

**验证清单**:
- [ ] ding.mp3 加入 SW 预缓存数组 + CACHE_VERSION 升级
- [ ] 摄像头扫码 + 手动输码两条路都有声
- [ ] 扫码成功有动画
- [ ] 飞行模式下音效正常
- [ ] 测试 checklist + PROGRESS 更新

---

## [√] V1-04. 通关音效

**前置**:V1-03(复用音频解锁 + 缓存确认)
**预计时间**:0.5-1 天
**类型**:游戏端(通关仪式感)

> 准备 1 个「通关庆祝」音效(< 150KB,mp3)放 `play/assets/sounds/victory.mp3`。

**【复制给 Claude Code 的指令】**

```
通关音效。victory.mp3 我已放进 play/assets/sounds/(还没提交,commit 时记得 git add)。
动 play/service-worker.js + play/pages/victory.html。

0. 先确认 play/assets/sounds/victory.mp3 在本地真实存在。

1. service-worker.js:
   - './assets/sounds/victory.mp3' 加进 PRECACHE_URLS。
   - CACHE_VERSION 版本号再 +1(若 V1-03 已升到 v2.6,这次 → 'treasure-hunt-runtime-v2.7')。

2. ⚠️ 关键:victory.html 是进页面自动播,不是点击触发;iOS 音频解锁【不跨页面传递】,
   上一页 photo.html 的手势到了 victory 新页面就失效。所以只在撒花时 victory.play(),
   iPad 上大概率被拦、.catch 静默吞掉 → 只见撒花没声音。做法:
   - 顶部建常驻 const victorySound = new Audio('../assets/sounds/victory.mp3')。
   - 撒花开始时先尝试 victorySound.play()(桌面/允许自动播的环境直接响)。
   - 同时挂一次性监听:页面首次 pointerdown/touchstart(once:true)时,若还没成功播过就补播一次。
     → iPad 上孩子一碰屏幕就响,撒花视觉不受影响。
   - 解锁/播放写法照搬 quiz.html 第 136-139 行那段(内联,无共享函数)。

3. 只播一次:用一个标志位防止「自动播 + 首次点击」重复播。刷新页面重新进算新的一次,可接受。
4. 不碰结算幂等(status/endedAt 只写一次)和撒花动画本身。音效默认开,不做开关。

5. 测试 checklist docs/test-checklists/V1-04-通关音效.md(按 §5.7 三段结构):
   - 桌面 Chrome 进通关页 → 撒花同时(或首次点击后)有声
   - iPad PWA 进通关页 → 撒花;点一下屏幕响一次,不重复刷屏
   - 飞行模式(先缓存过新 CACHE_VERSION)仍能播
   - iPad 三态

完成后(按顺序):
1. shared/version.js 的 BUILD_TAG 改 'V1-04-通关音效 · <今天日期>'(任务号 V1-04)
2. 更新 PROGRESS.md(V1-04 完成)
3. 单行英文 commit + push,带上 victory.mp3 / service-worker.js / victory.html / version.js / PROGRESS.md。
```

**验证清单**:
- [ ] 通关进入播放一次庆祝音效
- [ ] 飞行模式正常
- [ ] 测试 checklist + PROGRESS 更新

---

## [√] V1-05. 拼音标注(可切换显示)

**前置**:无
**预计时间**:2 天
**类型**:游戏端 + 新 CDN 库(辅助低龄孩子认字)

> 📌 **性能护栏**(实现时务必遵守):游戏每次只渲染一道题,所以**只转当前题**就够,别一进关卡把整关题都预转;一道题**整段一次调** `pinyin(text,{type:'array'})`,别一个字调一次。真出现切题卡顿再考虑缓存,先别提前优化。

**【复制给 Claude Code 的指令】**

```
给游戏端题干和选项加「拼音标注」,设置里可开关。用 pinyin-pro。
动 play/index.html、play/pages/settings.html、play/pages/quiz.html、play/service-worker.js。

【已替你核实,可放心】版本 3.18.2 存在;dist/index.js 是 UMD,普通 <script> 会挂出全局
pinyinPro;pinyinPro.pinyin('你好') = 'nǐ hǎo';pinyin(text,{type:'array'}) 默认就和字符
1:1 对齐(非汉字原样进数组)。包里【没有】dist/pinyin-pro.js,别改用那个名字(npm 路径会 404)。

第 1 步(仍在浏览器 Console 验证一次):
- 临时引入 <script src="https://cdn.jsdelivr.net/npm/pinyin-pro@3.18.2/dist/index.js"></script>
- Console 测 pinyinPro.pinyin('你好') → 'nǐ hǎo'。
- 万一 jsdelivr 抽风,备用 CDN:https://unpkg.com/pinyin-pro@3.18.2/dist/index.js

第 2 步(引入):
- 在 index.html 第 17 行 html5-qrcode 那行下面、和其他 CDN 库放一起,加:
  <script src="https://cdn.jsdelivr.net/npm/pinyin-pro@3.18.2/dist/index.js"></script>
  (固定版本,别 @latest)
- 不要加进 PRECACHE_URLS —— CDN 库由 SW 的 fetch 动态缓存,和 dexie 一样。
- 但要升 CACHE_VERSION(因为下面会改 index/quiz/settings 三个预缓存页):在当前字符串值
  'treasure-hunt-runtime-vX.Y' 基础上把版本号 +1。

第 3 步(设置开关):
- settings.html 加一个「显示拼音」开关(第一个 toggle,做个简单 checkbox/switch 即可)。
- 状态存 localStorage,键名 play-show-pinyin,默认 'on'(沿用 settings 里已有的 localStorage 用法)。

第 4 步(渲染,注意性能 + 转义):
- 写一个本地工具函数 renderPinyin(text) → 带拼音的 HTML:
  * 整段一次 pinyin(text, {type:'array'}) 拿逐字拼音(默认参数即可,别每字单独调)。
  * 用 Array.from(text) 遍历,和数组按同一索引对齐。
  * 每个汉字(/[\u4e00-\u9fa5]/)→ <ruby>字<rt>拼音</rt></ruby>;非汉字(标点/数字/字母/空格)
    原样输出、不标拼音。
  * ⚠️ 拼进 innerHTML 前,字符和拼音都要 HTML 转义(<、&、> 等),保留原来 textContent 的防 XSS 效果。
- quiz.html 渲染时读 localStorage 的 play-show-pinyin:
  * 题干(#question-text,第 183 行):开 → innerHTML = renderPinyin(question.text);关 → 维持
    textContent = question.text。
  * 选项:只对 .option-text span(第 193 行)做;开 → innerHTML = renderPinyin(optText);关 →
    textContent = optText。【别动】A/B/C/D 的 .option-label。
- 只转换当前这道题,别一次性转整关。
- 别在别处读 ruby 后的 DOM 当纯文字(它的 textContent 会带上拼音);正确答案/错题本继续用
  question.text、question.options[ci] 这些原始数据。
- <rt> 字号小、颜色淡,别太抢眼。

第 5 步(已知小坑,先告诉你别慌):
- 多音字偶尔标错(自动拼音通病),不过 pinyin-pro 靠词库其实大多对(银行→háng、重复→chóng 都对)。
  对孩子认字影响不大,先接受;以后某题明显错了再考虑加「手动拼音覆盖」字段,现在不做。

第 6 步:测试 checklist docs/test-checklists/V1-05-拼音.md(按 §5.7 三段结构):
- 开关打开 → 题干和每个选项文字上方有拼音;标点/数字/字母不带拼音;A/B/C/D 字母不带拼音
- 开关关闭 → 恢复纯文字;关掉再开,刷新后设置被记住
- iPad Mini(或浏览器窄屏)测【长选项 + 开拼音】→ 选项完整换行不截断、不卡
- iPad PWA 飞行模式(先联网缓存过新 CACHE_VERSION)→ 拼音仍显示
- F12 / Eruda 无红色错误;iPad 三态

完成后(按顺序):
1. shared/version.js 的 BUILD_TAG 改 'V1-05-拼音 · <今天日期>'(PowerShell Get-Date 取日期;任务号 V1-05)
2. 更新 PROGRESS.md(V1-05 完成)
3. 单行英文 commit + push,带上 index.html / settings.html / quiz.html / service-worker.js /
   version.js / PROGRESS.md。
```

**验证清单**:
- [ ] CDN 验证通过(全局名 pinyinPro)
- [ ] pinyin-pro@3.18.2 固定版本引入 + 被 SW 缓存
- [ ] 设置页「显示拼音」开关,localStorage 记忆,默认开
- [ ] 题干 + 选项逐字标拼音,非汉字不标;整段一次转、只转当前题
- [ ] 长选项 + 开拼音在窄屏不截断不卡
- [ ] 飞行模式下拼音可用
- [ ] 测试 checklist + PROGRESS 更新

---

## [ ] V1-06. 修两个已知 bug(B01 选项截断 + B02 飞行模式首次缓存)

**前置**:无(建议放在 V1-05 之后,B01 和拼音一起在窄屏测)
**预计时间**:1-2 天
**类型**:Bug 修复

**【复制给 Claude Code 的指令】**

```
修 known-issues.md 里的两个 bug:

B01:部分题目文字较长时,答题页选项文字被截断(iPad Mini 明显)。
- 检查 quiz.html 选项按钮的 CSS:去掉会截断的 overflow/固定高度/单行 ellipsis,
  改成可换行、按钮高度随内容自适应,长选项也能完整显示。
- 用一道很长的选项题在窄屏(iPad Mini 尺寸或浏览器调窄)验证不再截断;
  若 V1-05 拼音已做,顺便测【长选项 + 开拼音】不互相挤压。

B02:飞行模式首次启动时,若 Service Worker 没缓存完,CDN 资源可能失败。
- 这是「第一次必须联网让 SW 缓存住」的固有限制,不强求改成全本地,也【不要】粗暴到阻止进入游戏。
- 做一个友好提示:游戏端首次启动 / 导入数据后,如果检测到关键资源还没缓存,
  在设置页或启动页给一句:
  「⚠️ 第一次使用请先联网打开一遍,确保离线资源已下载,之后即可飞行模式游玩。」
- 检测可复用 SW 里已有的 CHECK_CACHE 消息机制(它能查离线资源备好没),不用从头写。
- 简单做法即可,不要为此重构 Service Worker。给我你打算怎么检测/提示的思路再动手。

分别写测试 checklist:docs/test-checklists/V1-06-bug修复.md。
完成后:commit + push;更新 PROGRESS.md + 在 known-issues.md 把 B01/B02 标记为已修。
```

**验证清单**:
- [ ] 长选项在窄屏不再截断(含开拼音时)
- [ ] 首次需联网的提示已加(文案友好,不阻止进入)
- [ ] known-issues.md 标记 B01/B02 已修
- [ ] 测试 checklist + PROGRESS 更新

---

## [ ] V1-07. 出题用字指南(纯文档，不写代码)

**前置**:无
**预计时间**:0.5 天
**类型**:文档(对应反馈 E08:孩子有些字不认识)

**【复制给 Claude Code 的指令】**

```
帮我在 docs/ 新建一份 docs/出题指南.md,给我自己出题时参考。内容简短实用:
- 按年龄段(4-6 / 7-9 / 10-12)给「用词难度」建议
- 提醒:低龄题尽量用常用字、短句;现在有拼音开关可辅助,但仍别堆生僻字
- 题干和选项长度建议(配合 V1-06 修的选项显示)
- 几个「好题 / 不好题」对照例子
- 提醒:解析字段可以写得像「讲给孩子听」那样口语化
不超过一页,给我能照着用的清单。
```

**验证清单**:
- [ ] docs/出题指南.md 创建,内容实用
- [ ] commit + push

---

## [ ] V1-08. A 区联调 + 真机三态验收

**前置**:V1-01 ~ V1-07 全部完成
**预计时间**:测试半天 + 修补 1-2 天
**类型**:阶段验收

**【你要做的事】**
1. 在编辑端给几道题补上「解析」,导出 JSON,AirDrop 到 iPad 导入。
2. iPad 上完整打一局(故意答错几题、用一次求助、跳过一次拍照):
   - [ ] 扫码成功有音效 + 动画
   - [ ] 答对/保底显示解析,答错显示提示
   - [ ] 通关有音效 + 撒花
   - [ ] 通关页有按站成绩单 + 错题本(得分/答对数(score>0)/错题详情含选错与正确答案)
   - [ ] 开/关拼音都正常;长选项不截断
   - [ ] **全程飞行模式跑通**(先联网缓存一次)
3. 续玩没被新功能破坏(答到一半退出能回到原处)。
4. 发现问题 → 整理后发我或丢给 Claude Code 修。
5. 全部通过 → commit + push → 更新 PROGRESS.md(A 区完成里程碑)。

**验证清单**:
- [ ] 完整一局所有 A 区功能正常
- [ ] iPad 飞行模式整局可玩
- [ ] 续玩未被破坏
- [ ] PROGRESS.md 标记 A 区完成

> ✅ A 区做完后,告诉我「**A 区做完了,展开 B 区**」,我把下面的提纲写成详细任务。

---

# 🟨 B 区：地图 + 故事 + 海报(V1-09 ~ V1-20，详细任务)

> 这是把原提纲展开后的可复制版本。**用法**:把 `任务清单_V1.md` 里原来 B 区那段提纲(从「# 🟨 B 区…提纲，做完 A 区再展开」到 V1-20 那行)整段替换成下面这份。C/D/E 区提纲不动。
>
> **B 区主线**:V1 的沉浸感(PRD §9)。L1 开场/结局故事 + L2 三段文案 + 探险地图 + 成就海报,一次到位。
>
> **本区建立在 MVP + A 区已有代码上,只增量加,不重写。** 涉及编辑端 `admin/`、游戏端 `play/`、共享层 `shared/`。
>
> 📌 **三个贯穿全区的约定(每个任务都默认遵守,不再重复写)**:
> 1. **字体先用系统字体**:地图名字、海报文字都用系统字体(微软雅黑/苹方),不引入卡通字体文件。美化以后再说。
> 2. **schema 章先别动**:V1-09 ~ V1-18 开发期间,导出 JSON 的 `schemaVersion` **仍然盖 "1.0"**。地图字段(mapImage 等)会顺带塞进导出文件里,旧的 1.0 导入逻辑会忽略多出来的字段、不报错。**只有到 V1-19 才同时把章改成 "1.1" + 放宽导入校验**,避免出现「盖了 1.1 章却自己导不回来」的空窗期。
> 3. **共享地图渲染模块**:V1-14 会新建一个小模块 `play/scripts/map-overlay.js`(就一个文件、两三个函数,**不是框架**),负责「按 mapX/mapY 在底图上画圈/名字/印章」。V1-15/16/17/18 全部复用它,不许各画各的。

---

## [√] V1-09. 编辑端：L1 表单解锁地图字段 UI(上传手绘图 + 字号/颜色配置)

**前置**:A 区全部完成
**预计时间**:1-1.5 天
**类型**:编辑端(B 区第一个任务,先把"地图能传进来"打通)

> 📌 **数据字段早就预留好了**(PRD §6.1):Level 上已有 `mapImage`(默认 null)、`mapFontSize`("medium")、`mapNameColor`("#2C3E50")、`mapNameColorCompleted`("#F5A623")。MVP 阶段这些字段在数据里但**编辑端完全不显示 UI**。这个任务就是**把这几个字段的输入框显示出来**,不新增数据字段、不动 schema、不动导出。
>
> ⚠️ **图片压缩很重要**:手绘图要 base64 直接进 JSON,不压会让 JSON 文件大到 AirDrop 都费劲。**上传时自动压到长边 ≤1600px**(MVP 合影压缩用的是 1280px,可参考同一套 canvas 压缩写法)。

**【复制给 Claude Code 的指令】**

```
给编辑端 L1 表单加「探险地图」相关字段的输入 UI。Level 数据模型上 mapImage / mapFontSize /
mapNameColor / mapNameColorCompleted 这几个字段早就预留了(默认值见 PRD §6.1),现在只是把输入
界面显示出来,不新增字段、不动 schema、不动导出逻辑。

【动手前先确认】
1. 先在仓库里找到编辑端 L1 的新建/编辑表单是哪个文件(admin/pages/ 下,可能是 levels.html 或
   level-edit 之类),以及保存 L1 时把表单值写进 Level 对象的那段代码。先告诉我文件名和大致位置。
2. console.log 一个现有 Level 对象,确认上面真有 mapImage/mapFontSize/mapNameColor/
   mapNameColorCompleted 这四个字段。没有先告诉我,别硬写。

在 L1 表单里(放在现有字段下方一个「探险地图(可选)」分组里)加:

1. 手绘地图上传:
   - 一个 <input type="file" accept="image/*">。
   - 选图后用 canvas 压缩:长边 ≤1600px,导出 JPEG(quality 0.8 左右),转成 base64 字符串
     存进 mapImage。压缩写法可参考游戏端合影压缩那段(那边是 1280px,这里改 1600)。
   - 上传后显示缩略图预览 + 一个「移除地图」按钮(把 mapImage 设回 null)。
   - 旁边小字提示:「只画风景/图标/路线就行,点位的圈、名字、印章都是游戏里自动画的,不用你画。」

2. 地图字号 mapFontSize:三选一(小 small / 中 medium / 大 large),默认中。

3. 名称默认色 mapNameColor:颜色选择器,默认 #2C3E50。小字「点位'已发现还没完成'时名字的颜色」。

4. 名称高亮色 mapNameColorCompleted:颜色选择器,默认 #F5A623(金色)。小字「点位'已通关盖章后'名字变成的颜色」。

5. 保存 L1 时把这四个值写回 Level 对象(没传图就 mapImage = null,其余给默认值)。

注意:
- 不要动导出逻辑,也不要把 schemaVersion 改成 1.1(那是 V1-19 的事)。这几个字段会自然跟着
  现有 exportFull 一起导出,现在这样就对。
- 多写中文注释,函数 20-30 行内。

写测试清单 docs/test-checklists/V1-09-地图上传.md(按 CLAUDE.md §5.7 三段结构,编辑端只需桌面 Chrome + iPad Safari 两段):
- 传一张较大的手绘图(比如 3000px 宽)→ 保存后再打开,缩略图还在,且 F12 看 mapImage 是
  压缩后的 base64(长边 ≤1600)
- 改字号/两个颜色 → 保存 → 重新打开值被记住
- 「移除地图」→ mapImage 变回 null
- 不传图直接保存 L1 → 不报错,mapImage = null(向后兼容老 L1)
- F12 Console 无红色错误

完成后(按顺序):
1. shared/version.js 的 BUILD_TAG 改 'V1-09-地图上传 · <今天日期>'(PowerShell Get-Date -Format 'yyyy-MM-dd';任务号 V1-09)
2. 更新 PROGRESS.md(V1-09 完成)
3. 单行英文 git commit + push
```

**验证清单**:
- [ ] L1 表单有「上传地图」+ 字号 + 两个颜色的输入,值能存能读
- [ ] 上传时自动压到长边 ≤1600px(base64 进 mapImage)
- [ ] 不传图的老 L1 不报错(mapImage = null)
- [ ] 没动 schemaVersion / 导出逻辑
- [ ] 测试 checklist + version.js + PROGRESS 更新

---

## [√] V1-10. 编辑端：地图标记编辑器(桌面点击放置 + 拖动 + 删除 + 预览)

**前置**:V1-09(要先能上传地图)
**预计时间**:2-3 天(B 区最费交互的一块)
**类型**:编辑端(纯桌面交互,iPad 上禁用)

> 📌 **坐标用百分比存**(PRD §4.1.8 + §6.2):每个 L2 的位置存成 `mapX / mapY`,值是 0~1 的百分比(比如 x:0.35 表示在图片宽度 35% 处),这样地图缩放到任何尺寸都对得上。mapX/mapY 字段 Point 上已预留,默认 null。
>
> ⚠️ **iPad 不支持触摸编辑**:这是 PRD 明确的限制。iPad 打开要显眼提示「📍 地图标记请在电脑上编辑」并禁用拖拽,别让家长在 iPad 上戳半天戳不准。

**【复制给 Claude Code 的指令】**

```
给编辑端做「地图标记编辑器」:家长在电脑上,在已上传的手绘图上点击放置每个 L2 的坐标。
坐标按百分比存进对应 Point 的 mapX/mapY(字段已预留,默认 null,0~1 小数)。这是桌面专用功能。

【动手前先确认】
1. 找到 L1 详情页是哪个文件,以及怎么拿到该 L1 下的 L2 列表
   (应该是 db.points.where('levelId').equals(levelId).sortBy('order'))。先告诉我。
2. console.log 一个 Point,确认有 mapX/mapY 字段(默认 null)。

入口:
- 在 L1 详情页加一个「📍 地图标记」按钮,【只在该 L1 已上传地图(mapImage 非 null)时才显示】。
- 点击打开标记编辑器(可以是同页弹层,也可以是单独页面 admin/pages/map-marker.html,你选简单的那种,
  告诉我你选哪个)。

编辑器界面(桌面布局):
- 左侧:地图图片显示区,按容器宽度自适应缩放显示 mapImage。
- 右侧:操作面板 —— 预览开关、清空所有按钮、L2 标记列表(显示每个 L2 名字 + 已标/未标)。

交互(鼠标):
- 点击图片【空白处】放置标记:按 L2 的 order 顺序依次绑定。
  即「还没标坐标的 L2 里 order 最小的那个」拿到这次点击的坐标。
  把点击位置换算成百分比:mapX = 点击x / 图片显示宽度,mapY = 点击y / 图片显示高度(0~1)。
- 拖动已有标记:鼠标按住标记拖到新位置,松开时更新该 L2 的 mapX/mapY。
- 点击已有标记:弹一个小菜单(删除 / 取消)。删除 = 把该 L2 的 mapX/mapY 设回 null。
- 预览开关:
  * 关(默认,编辑视图):每个标记画成带数字的圆圈(1/2/3…,数字 = L2 的 order),方便家长对位。
  * 开(游戏预览):画成游戏端「已发现未完成」的样子(白色空心圆 + L2 名字,名字用 mapNameColor 颜色),
    模拟孩子会看到的效果。
- 清空所有:二次确认后,把该 L1 所有 L2 的 mapX/mapY 设回 null。

保存规则(PRD §4.1.8):
- 必须【所有 L2 都标了坐标】才能保存这次标记。否则提示「还有 X 个点位没标坐标,无法保存」。
- 保存就是把各 L2 的 mapX/mapY 写回 IndexedDB(记得打开 Dexie 前调 assertDbContext)。

iPad/触摸屏处理(重要):
- 检测到触摸设备(没有精确鼠标,比如 matchMedia('(pointer: coarse)') 命中)时:
  显眼横幅提示「📍 地图标记请在电脑上编辑」,并【禁用拖拽和点击放置】(可以只读展示已标好的标记)。

简单优先:别上拖拽库,原生 mousedown/mousemove/mouseup 就够。一个标记一个绝对定位的小 div。
多写中文注释。

写测试清单 docs/test-checklists/V1-10-地图标记.md(桌面 Chrome + iPad Safari 两段):
- 桌面:依次点击放置,1/2/3… 顺序对;拖动能微调;点标记能删除(变回未标)
- 预览开关:关=数字圈,开=空心圈+名字
- 没标完就保存 → 被拦 + 提示还差几个
- 全标完保存 → 重新打开编辑器,坐标还在位置对
- 清空所有 → 二次确认 → 全部清空
- iPad Safari 打开 → 出现「请在电脑上编辑」提示 + 不能拖
- F12 无红色错误

完成后(按顺序):
1. shared/version.js BUILD_TAG 改 'V1-10-地图标记 · <今天日期>'(任务号 V1-10)
2. 更新 PROGRESS.md(V1-10 完成)
3. 单行英文 commit + push
```

**验证清单**:
- [ ] 点击按 L2 order 放置;拖动微调;点击删除单个
- [ ] 坐标按百分比(0~1)存进 mapX/mapY
- [ ] 预览开关:编辑视图(数字圈)/ 游戏预览(空心圈+名字)
- [ ] 没标完不让保存;清空所有有二次确认
- [ ] iPad 显「请在电脑上编辑」+ 禁用拖拽
- [ ] 测试 checklist + version.js + PROGRESS 更新

---

## [√] V1-11. 编辑端：L2 三段自定义文案字段(补 questionIntroText + completionText)

**前置**:无(可与 V1-09/10 独立,但顺序上仍排在这)
**预计时间**:0.5-1 天
**类型**:编辑端(纯加表单字段)

> 📌 **发现文案 discoveryText 已经做好了**(MVP 阶段就加了输入框,PRD §6.2 标注「MVP 已做」)。这个任务只补**另外两段**:答题引导 `questionIntroText`、通关庆祝 `completionText`。两个字段 Point 上已预留(默认空串)。三段文案都**柔性可空**——空着游戏端就不弹对应弹窗。

**【复制给 Claude Code 的指令】**

```
给编辑端 L2 表单补两个文案输入框。Point 上 questionIntroText / completionText 字段已预留(默认 "")。
发现文案 discoveryText 之前 MVP 已经做了,不用动。

【动手前先确认】
找到 L2 新建/编辑表单是哪个文件,以及现在 discoveryText 输入框在哪一段(新字段就放它附近)。
确认 Point 上有 questionIntroText / completionText 两个字段。告诉我位置。

在 discoveryText 输入框下面,加两个多行文本框,放在一个「孩子看到的文案(都可留空)」分组里:
1. 答题引导 questionIntroText —— 小字说明:「扫码、地图动画之后,开始答题前弹给孩子看(比如:仔细看看周围,答出 3 道题就能继续闯关!)。留空就不弹。」
2. 通关庆祝 completionText —— 小字说明:「这一站全部答完时弹给孩子看(比如:太棒了!这一站攻克啦!)。留空就不弹。」

三段文案顺手在分组开头给一句总说明,让家长明白它们出现的顺序:
「发现文案 → (地图动画) → 答题引导 → 答题 → 通关庆祝 → (盖章动画)」。

保存 L2 时把这两个值写回 Point。空着就是空串,不报错。不动 schema、不动导出。
多写中文注释。

写测试清单 docs/test-checklists/V1-11-三段文案.md(桌面 + iPad Safari):
- 两个框都填 → 保存 → 重开值还在
- 只填一个 / 都留空 → 保存不报错
- 老 L2(没填过)→ 打开不报错,显示空框
- F12 无红色错误

完成后(按顺序):
1. shared/version.js BUILD_TAG 改 'V1-11-三段文案 · <今天日期>'(任务号 V1-11)
2. 更新 PROGRESS.md(V1-11 完成)
3. 如果本次改动了任何 .js 或 .css 文件,把引用它的 HTML 里对应的 ?v= 版本号一起改成本次任务号;没有 ?v= 引用的文件则忽略此条。
4. 单行英文 commit + push
```

**验证清单**:
- [ ] L2 表单新增 questionIntroText + completionText 两个多行框(discoveryText 不动)
- [ ] 都可留空,空=空串,不报错;老 L2 不报错
- [ ] 没动 schema / 导出
- [ ] 测试 checklist + version.js + PROGRESS 更新

---

## [√] V1-12. 编辑端：L1 开场 + 结局故事文本输入

**前置**:无
**预计时间**:0.5 天
**类型**:编辑端(纯加表单字段)

> 📌 字段 `openingStory` / `endingStory` Level 上已预留(默认空串,PRD §6.1)。这任务只把两个多行框显示出来。**填了游戏端才显示**(V1-13 负责显示)。

**【复制给 Claude Code 的指令】**

```
给编辑端 L1 表单加两个故事文本框:openingStory(开场故事)、endingStory(结局故事)。
两个字段 Level 上已预留(默认 "")。

【动手前先确认】
找到 V1-09 改过的同一个 L1 表单文件,确认 Level 有 openingStory / endingStory 字段。

在 L1 表单加一个「探险故事(可选)」分组,两个多行文本框:
1. 开场故事 openingStory —— 小字:「孩子进入这个探险时先读到的故事开头。留空就直接开始,不显示故事页。」
2. 结局故事 endingStory —— 小字:「全部通关后显示的故事结尾。留空就不显示。」

保存 L1 时写回 Level。空=空串,不报错。不动 schema、不动导出。多写中文注释。

写测试清单 docs/test-checklists/V1-12-开场结局故事.md(桌面 + iPad Safari):
- 两个都填 → 保存 → 重开还在
- 留空 → 不报错;老 L1 不报错
- F12 无红色错误

完成后(按顺序):
1. shared/version.js BUILD_TAG 改 'V1-12-开场结局故事 · <今天日期>'(任务号 V1-12)
2. 更新 PROGRESS.md(V1-12 完成)
3. 如果本次改动了任何 .js 或 .css 文件,把引用它的 HTML 里对应的 ?v= 版本号一起改成本次任务号;没有 ?v= 引用的文件则忽略此条。
4. 单行英文 commit + push
```

**验证清单**:
- [ ] L1 表单新增 openingStory + endingStory 两个多行框,值能存能读
- [ ] 可留空;老 L1 不报错;没动 schema / 导出
- [ ] 测试 checklist + version.js + PROGRESS 更新

---

> 👇 **以下转入游戏端**(play/)。编辑端四块(V1-09~12)做完、真机过一遍再开始。

---

## [√] V1-13. 游戏端：开场 + 结局故事页(填了才显示)

**前置**:V1-12(要先有故事数据可读)
**预计时间**:0.5-1 天
**类型**:游戏端

> 📌 **MVP 明确没读 openingStory**(PRD §4.2.1 写明「MVP 不读 openingStory 字段」)。这任务正式启用故事页。向后兼容:没填故事的 L1 直接跳过故事页(PRD §4.2.10 向后兼容)。

**【复制给 Claude Code 的指令】**

```
游戏端启用开场/结局故事页。openingStory 在进入探险、正式开始前显示;endingStory 在全部通关时显示。
两者【填了才显示,空就跳过】。

【动手前先确认】
1. 找到「点了探险卡片 → 开始冒险」之间的流程是哪个文件/页面(PRD §4.2.1/4.2.2)。
2. 找到通关庆典页(应该是 victory.html)。
3. console.log 当前 level 对象,确认能拿到 openingStory / endingStory。

开场故事:
- 进入某个探险、显示「开始冒险!」之前,如果 level.openingStory 非空:
  先显示一个故事页(一段大字故事 + 一个「开始冒险!」按钮),点了再进第 1 站提示页。
- openingStory 为空 → 维持现在的流程,不显示故事页(向后兼容)。

结局故事:
- 在通关庆典页(victory.html),如果 level.endingStory 非空:
  在合适位置(撒花之后、成绩单之前或之后,你选视觉顺的)显示结局故事文本。
- 为空 → 不显示,不留空白。

注意:
- 故事文本可能有换行,渲染时保留换行(white-space: pre-wrap 之类)。
- 不碰 A 区做的成绩单/错题本、不碰结算幂等(status/endedAt 只写一次)、不碰撒花。
- 字大、可读、留白舒服(给孩子读的)。

写测试清单 docs/test-checklists/V1-13-故事页.md(按 §5.7 三段结构):
- 填了开场故事的探险 → 进入先看到故事页 → 点按钮才开始
- 没填开场 → 直接开始,不卡故事页
- 填了结局 → 通关页能看到;没填 → 不显示空块
- 多行故事换行正常
- iPad 三态;Eruda 无红色错误

完成后(按顺序):
1. shared/version.js BUILD_TAG 改 'V1-13-故事页 · <今天日期>'(任务号 V1-13)
2. 更新 PROGRESS.md(V1-13 完成)
3. 如果本次改动了任何 .js 或 .css 文件,把引用它的 HTML 里对应的 ?v= 版本号一起改成本次任务号;没有 ?v= 引用的文件则忽略此条。
4. 单行英文 commit + push
```

**验证清单**:
- [ ] 有开场故事 → 进探险先显示故事页;无 → 跳过
- [ ] 有结局故事 → 通关页显示;无 → 不显示
- [ ] 换行正常;没破坏成绩单/撒花/结算幂等
- [ ] iPad 三态 + 测试 checklist + version.js + PROGRESS 更新

---

## [√] V1-14. 游戏端：扫码后「发现文案」弹窗 + 地图浮现动画(建共享渲染模块)

**前置**:V1-09、V1-10(要有地图和坐标)、V1-11(answer/discovery 文案)
**预计时间**:2-3 天(本任务要顺手建共享地图渲染模块,后面几个都靠它)
**类型**:游戏端 + 新建共享模块

> 📌 **这是地图在游戏里第一次出现的任务,所以在这里建共享渲染模块** `play/scripts/map-overlay.js`。它负责:给定 level(底图+字号+颜色) + points(含 mapX/mapY/名字) + 每个点位的当前状态(未发现/已发现/已完成),在一个容器里把底图 + 圈 + 名字 + 印章画出来。V1-15/16/17/18 全部复用它。**就一个文件、几个函数,别搭框架。**
>
> 📌 **扫码后的完整流程**(PRD §4.2.3 步骤 3a)——三段文案柔性逻辑:
> - 都填:发现弹窗 → 地图浮现动画 → 答题引导弹窗 → 答题
> - 只填发现文案:发现弹窗 → 地图动画 → 自动进答题
> - 只填答题引导:地图动画 → 引导弹窗 → 答题
> - 都不填:地图动画 → 自动进答题
> - **整个 L1 没传地图:跳过地图动画**,扫码后直接按文案有没有决定弹不弹窗
>
> 📌 **本任务负责的状态变化是「浮现」**:扫码后该点位从「未发现」变「已发现未完成」=白色空心圈 + 名字(默认色),**不盖章**。盖章是 V1-15 的事。

**【复制给 Claude Code 的指令】**

```
做两件相关的事:(1) 新建共享地图渲染模块;(2) 把它接到扫码成功之后的流程里,做「发现文案弹窗 +
地图浮现动画」。

【动手前先确认】
1. 找到扫码/输码成功后、进入答题之前的代码(MVP 是 scan.html 成功后跳 quiz.html,或 quiz 里有
   "开始挑战"页)。先把这条路径理清楚告诉我:扫码成功在哪触发、怎么进答题。
2. 确认能拿到:当前 level(含 mapImage/mapFontSize/mapNameColor/mapNameColorCompleted)、
   当前 L1 所有 points(含 mapX/mapY/name)、当前 session(知道哪些 L2 已发现/已完成)。
3. discoveryText(MVP 已有)、questionIntroText(V1-11 新增)在 point 上,确认能读到。

═══ 第 1 部分:新建 play/scripts/map-overlay.js(共享渲染模块)═══

写一个不碰 DB 的纯渲染模块,导出大概这几个函数(名字你可定,保持简单):
- renderMapOverlay(container, level, points, stateMap):
  * container 里先放底图 <img src=level.mapImage> 自适应铺满。
  * 遍历 points,对每个有 mapX/mapY 的点位,按百分比换算成容器内像素位置,绝对定位一个标记 div。
  * stateMap 给出每个 pointId 的状态:'undiscovered' / 'discovered' / 'completed'。
    - undiscovered:什么都不画(只露底图)
    - discovered:白色空心圆 + 名字(颜色 = level.mapNameColor)
    - completed:红色五角星印章 + 名字(颜色 = level.mapNameColorCompleted)
  * 名字字号按 level.mapFontSize(small/medium/large)给三档 px。字体用系统字体,不引字体文件。
  * 五角星用内联 SVG(红色,可旋转 -15°),别引图片。
- 留一个能"播放浮现动画"和"播放盖章动画"的入口(下面第 2 部分 + V1-15 用),
  比如 renderMapOverlay 接一个可选参数 animatePointId + animateType('reveal'|'stamp')。
  动画用 CSS class + keyframes,别用动画库。

保持简单:一个文件,几个函数,纯 DOM + CSS,不连数据库,不依赖具体页面。多写中文注释。

═══ 第 2 部分:接到扫码成功流程(PRD §4.2.3 步骤 3a)═══

扫码/输码成功后,按这个柔性逻辑走(三段文案任意可空):
1. 若 point.discoveryText 非空 → 先弹「发现文案」弹窗(文案 + 「继续」按钮),点继续再往下。
2. 若 level 有地图(mapImage 非空):
   全屏弹出地图覆盖层,用 map-overlay 渲染,并对【当前这个点位】播放「浮现」动画
   (空心圆 + 名字从透明淡入,约 0.5 秒,该点位状态置为 discovered)。
   动画结束 0.5 秒后:
     - 若 point.questionIntroText 非空 → 关掉地图,弹「答题引导」弹窗(文案 + 「开始答题」按钮)→ 进答题
     - 否则 → 自动关地图进答题
   若 level 没有地图 → 跳过地图动画,直接:有答题引导就弹引导→答题,没有就直接进答题。
3. 状态来源:已发现/已完成要根据 session 里已完成的 L2 推出来,保证「之前完成的站打开地图也显示对」。

注意:
- 别动答题判分、求助、保底逻辑。
- 别动 MVP 的「这不是你要找的点」「无效码」提示。
- 动画别太久,别卡;孩子等不及。

写测试清单 docs/test-checklists/V1-14-发现浮现.md(按 §5.7 三段结构):
- 三段文案都填 → 发现弹窗→地图浮现→引导弹窗→答题,顺序对
- 只填发现文案 → 发现弹窗→浮现→自动进答题
- 只填引导 → 浮现→引导→答题
- 都不填 → 浮现→自动进答题
- L1 没传地图 → 跳过地图动画,按文案决定弹窗
- 之前已完成的站,这次打开地图也正确显示(盖章态/已发现态)
- iPad 三态;Eruda 无红色错误

完成后(按顺序):
1. shared/version.js BUILD_TAG 改 'V1-14-发现浮现 · <今天日期>'(任务号 V1-14)
2. 更新 PROGRESS.md(V1-14 完成 + 记一句"新建 map-overlay.js 共享渲染模块")
3. 如果本次改动了任何 .js 或 .css 文件,把引用它的 HTML 里对应的 ?v= 版本号一起改成本次任务号;没有 ?v= 引用的文件则忽略此条。
4. 单行英文 commit + push
```

**验证清单**:
- [ ] 新建 `play/scripts/map-overlay.js`,纯渲染、不连 DB
- [ ] 三态渲染正确(未发现不画 / 已发现空心圈+默认色名 / 已完成五角星+高亮色名)
- [ ] 扫码后三段文案柔性逻辑全对(四种组合 + 无地图)
- [ ] 浮现动画约 0.5 秒,只动当前点位
- [ ] iPad 三态 + 测试 checklist + version.js + PROGRESS 更新

---

## [√] V1-15. 游戏端：答完题「通关庆祝」弹窗 + 地图盖章动画(红五角星 + 名字变金)

**前置**:V1-14(复用 map-overlay + completion 文案)
**预计时间**:1-2 天
**类型**:游戏端

> 📌 **流程**(PRD §4.2.3 步骤 6 + §4.2.10 盖章动画,**V1-A 改为内嵌式,与 V1-14 发现页一致,不再用全屏弹窗**):一个 L2 所有题答完 → 跳到拍照页(photo.html)。在拍照页「第 N 站完成」文案与「📸 拍照 / 跳过」按钮**之间内嵌**地图;若有 completionText 一并显示在文案区。地图对当前点位播放盖章动画(红色五角星从上方砸下 + 轻微震动 + 名字从默认色变金色,约 0.8 秒),**盖完地图留在页面上不消失、不跳转**。**盖章 = 当前站状态从 discovered 变 completed。**

**【复制给 Claude Code 的指令】**

```
在拍照页(photo.html)的「拍照选择屏」里把地图内嵌进去(放在「第 N 站完成」文案和
「📸 拍照 / 跳过」按钮之间),对当前这一站播放盖章动画。复用 V1-14 建的 map-overlay.js,
别另写一套渲染,也别再用全屏弹窗(V1-A 已统一改成内嵌式,和 V1-14 发现页一致)。

【动手前先确认】
1. 这一站「最后一题答完」后会跳到 photo.html 的 screen-prompt(「第 N 站完成!」+ 📸拍照/跳过)。
   确认这条跳转路径,以及 photo.html 怎么从 session 拿当前站、当前 level、所有点位。
2. 确认能读到 point.completionText(V1-11)和当前 level 地图配置
   (mapImage/mapFontSize/mapNameColor/mapNameColorCompleted)。

要做的事(都在 photo.html 的拍照选择屏内,不开新弹窗):
1. completionText 内嵌:若 point.completionText 非空,显示在「第 N 站完成」文案区下方
   (和 V1-14 把 discoveryText/questionIntroText 合进发现页一样的做法,纯文本,不弹窗)。
2. 内嵌地图:若 level 有 mapImage,在文案区和「📸拍照/跳过」按钮之间内嵌一个地图容器,
   用 map-overlay 渲染,naturalHeight:true 去黑边(同 V1-14)。
   - 状态图要把【当前这一站】算作 completed。注意:拍照页此时 session.currentPointIndex
     还指向这一站(要到点「继续」才推进),所以这里的 stateMap 不能照搬 quiz.html 的
     「当前 index = discovered」,要让当前站及之前的站都算 completed。
   - 对当前这一站播放盖章动画 animateType='stamp':红色五角星 SVG(-15°)从上方"咚"砸到圆圈
     位置 + 落点轻微震动 + 名字从默认色变金色(mapNameColorCompleted),总时长约 0.8 秒。
   - 盖完地图留在页面上,不自动消失、不跳转(孩子接着点拍照或跳过)。
   - 若 level 没有 mapImage → 不显示地图,照原样只显示拍照提示。
3. 拍照 / 跳过 / 继续 流程照旧,别动。

注意:
- 盖章状态从「已完成的 L2」推出来即可,保证后面打开地图/通关页这站显示已完成。
  但【别碰结算幂等】——L1 整体的 status/endedAt 只在通关时写一次,这里只是单站状态。
- 圈和名字的"浮现"是 V1-14 在发现页做的;这里只做"盖章+变金"。两次动画各管各的,别重复浮现。
- 别动拍照流程、判分、撒花。

写测试清单 docs/test-checklists/V1-15-盖章动画.md(按 §5.7 三段结构):
- 填了 completionText → 拍照页文案区显示该文案 + 下方地图盖章动画 → 接着拍照/跳过
- 没填 → 拍照页只有「第 N 站完成」+ 地图盖章动画 → 拍照/跳过
- L1 没地图 → 拍照页不显示地图,直接拍照提示
- 盖章后地图留在拍照页;再打开地图(V1-16)/看通关页,这站是五角星+金名(completed)
- 盖章动画流畅、约 0.8 秒、不卡;地图无黑边
- iPad 三态;Eruda 无红色错误

完成后(按顺序):
1. shared/version.js BUILD_TAG 改 'V1-15-盖章动画 · <今天日期>'(任务号 V1-15)
2. 更新 PROGRESS.md(V1-15 完成)
3. 如果本次改动了任何 .js 或 .css 文件,把引用它的 HTML 里对应的 ?v= 版本号一起改成本次任务号;没有 ?v= 引用的文件则忽略此条。
4. 单行英文 commit + push
```

**验证清单**:
- [ ] 拍照页文案与「📸拍照/跳过」按钮之间内嵌地图(不是全屏弹窗、不自动消失)
- [ ] 有 completionText 时内嵌显示在文案区(不弹窗)
- [ ] 盖章动画(红星砸下+震动+名字变金,~0.8s),只动当前站
- [ ] 复用 map-overlay 的盖章入口,没另写渲染;naturalHeight 去黑边
- [ ] 当前站按 completed 渲染(拍照页 stateMap 正确,不显示成 discovered)
- [ ] 盖章后状态保持 completed,不破坏 L1 结算幂等
- [ ] 无地图的 L1 跳过地图
- [ ] iPad 三态 + 测试 checklist + version.js + PROGRESS 更新

---

## [√] V1-16. 游戏端：🗺️ 浮动地图按钮(随时呼出地图,避开右上角求助按钮)

**前置**:V1-14(复用 map-overlay)
**预计时间**:0.5-1 天
**类型**:游戏端

> 📌 PRD §4.2.10:浮动按钮在**屏幕右下角**(避开右上角求助按钮),出现在 L2 提示页 / 答题页。点击全屏打开地图(只看,不触发动画),再点关闭。
>
> 📌 **设计已变**(V1-14/15):发现页(quiz 发现屏)和拍照页(photo.html)已经**内嵌**地图了,这两处不必再放浮动按钮;原 V1-15 的「通关庆祝弹窗」也已不存在(改成拍照页内嵌)。全屏查看地图时复用 `naturalHeight` 模式(去黑边、整图一屏,和内嵌地图视觉一致)。

**【复制给 Claude Code 的指令】**

```
加一个 🗺️ 浮动地图按钮,孩子随时点开看当前进度地图。复用 V1-14 的 map-overlay 渲染。

【动手前先确认】
找到 L2 提示页(hint.html)、答题页(quiz.html 答题屏)这两处,确认右上角是求助按钮的位置。
(发现页、拍照页已内嵌地图,跳过。)确认能拿到 level + points + 当前 session 状态。

实现:
1. 在这两页加一个固定定位的 🗺️ 圆按钮,放【右下角】(明确避开右上角求助按钮,别打架)。
   - 出现在:L2 提示页(hint.html)、答题页(quiz.html 答题屏)
   - L1 没传地图(mapImage 为空)→ 不显示这个按钮
2. 点击 → 全屏地图覆盖层,用 map-overlay 按【当前真实状态】渲染(已完成的盖章、已发现的空心圈、
   没发现的不画),【不播放任何动画】(纯查看)。渲染用 naturalHeight 模式(去黑边、整图一屏,
   和 V1-14/15 内嵌地图视觉一致)。右上角一个关闭按钮,点了关掉回原页面。
3. 按钮够大(≥ 60×44,符合 §9 视觉规范),别挡住题目和选项。

注意:别动判分/求助/拍照。这个按钮是"只读看地图",不改任何状态。

写测试清单 docs/test-checklists/V1-16-浮动地图按钮.md(按 §5.7 三段结构):
- 提示页/答题页都能看到 🗺️ 按钮,在右下角,不和右上角求助按钮重叠
- 点开 → 看到当前进度地图(状态正确)→ 关闭回原页
- 答到一半点开地图再关 → 题目状态没乱(没误判/没重置)
- 无地图 L1 → 不显示按钮
- iPad 三态;Eruda 无红色错误

完成后(按顺序):
1. shared/version.js BUILD_TAG 改 'V1-16-浮动地图按钮 · <今天日期>'(任务号 V1-16)
2. 更新 PROGRESS.md(V1-16 完成)
3. 如果本次改动了任何 .js 或 .css 文件,把引用它的 HTML 里对应的 ?v= 版本号一起改成本次任务号;没有 ?v= 引用的文件则忽略此条。
4. 单行英文 commit + push
```

**验证清单**:
- [ ] 🗺️ 按钮在右下角,提示页/答题页都有,不撞求助按钮
- [ ] 点开按当前真实状态渲染(naturalHeight 去黑边)、纯查看不触发动画、能关闭
- [ ] 打开/关闭不影响答题状态;无地图 L1 不显示
- [ ] iPad 三态 + 测试 checklist + version.js + PROGRESS 更新

---

## [√] V1-17. 游戏端：地图三态渲染统一核对(未发现 / 已发现 / 已完成)

**前置**:V1-14、V1-15、V1-16
**预计时间**:0.5-1 天(主要是统一核对 + 修边角,不是重写)
**类型**:游戏端(收口验证)

> 📌 三态其实已经在 V1-14 的 map-overlay 里实现了。这个任务是**统一核对所有呼出口的状态都一致**(扫码浮现、盖章后、浮动按钮、刷新续玩后),把状态判断收成一处真相来源,别出现「同一个站在不同页面显示不一样」的 bug。**不重写,补齐 + 核对。**

**【复制给 Claude Code 的指令】**

```
统一核对地图三态在所有入口都一致。三态渲染逻辑 V1-14 已写在 map-overlay,这里不重写,只做收口:

【动手前先确认】
列出现在所有会渲染/呼出地图的地方:开场故事页全览(start-level.html)、扫码后发现页浮现(V1-14,
quiz.html 发现屏)、答完拍照页盖章(V1-15,photo.html)、浮动按钮全屏查看(V1-16)、
通关页/海报(V1-18 还没做先不管)。告诉我各处现在分别怎么算"某个点位是什么状态"的。
(目前 quiz.html / photo.html / start-level.html 三处各写了一套 stateMap,正是本任务要统一的。)

要做的:
1. 把"某个 pointId 现在是 undiscovered / discovered / completed"收成【一个共用函数】
   (放 map-overlay.js 或 session 工具里),所有入口都调它,别各算各的。判断依据:
   - completed:该 L2 在本局已答完(session 里有该 L2 的完成记录)
   - discovered:该 L2 已扫码进入过、但还没答完(若你们没单独记"已扫未完",可简化为
     "当前正在玩的这一站算 discovered",其余未完成的算 undiscovered —— 你看现有 session 数据
     能支持到哪种,告诉我再定,别硬塞字段)
   - undiscovered:其余
   ⚠️ **拍照页(V1-15)这个特例必须用"是否答完"判断,不能用 index==current**:进拍照页时该站
   所有题已答完,但 session.currentPointIndex 还指向它(点「继续」才推进)。若沿用 quiz.html 的
   "index==current → discovered",刚答完的站在拍照页会错显示成空心圈而非盖章。共用函数判 completed
   要依据"该站是否答完(pointRecords 有完成记录)",这正是收口要消除的不一致(现在 photo.html 是
   单独用 i<=current 绕过的,统一后改成走共用函数)。
2. 核对三种状态视觉符合 PRD §4.2.10:
   - 未发现:只露底图,不画圈/名/章
   - 已发现未完成:白色空心圆 + 名字(mapNameColor)
   - 已完成:红色五角星 + 名字(mapNameColorCompleted 金色)
3. 重点核对"续玩":答了几站后退出 App、再进来续玩,打开地图,已完成的站仍是盖章态(状态从
   session/已完成记录推出来,不是只存在内存里)。

写测试清单 docs/test-checklists/V1-17-地图三态.md(按 §5.7 三段结构):
- 一局里:没到的站(未发现)不画东西;当前/已发现站空心圈;已通关站盖章金名
- 扫码后 vs 盖章后 vs 浮动按钮打开,同一个站状态一致
- 退出 App 再续玩 → 打开地图,已完成的站仍盖章(状态没丢)
- iPad 三态;Eruda 无红色错误

完成后(按顺序):
1. shared/version.js BUILD_TAG 改 'V1-17-地图三态 · <今天日期>'(任务号 V1-17)
2. 更新 PROGRESS.md(V1-17 完成)
3. 如果本次改动了任何 .js 或 .css 文件,把引用它的 HTML 里对应的 ?v= 版本号一起改成本次任务号;没有 ?v= 引用的文件则忽略此条。
4. 单行英文 commit + push
```

**验证清单**:
- [ ] 状态判断收成一个共用函数,各入口都调它
- [ ] 三态视觉符合 PRD;扫码/盖章/浮动按钮三处显示一致
- [ ] 续玩后已完成站仍盖章(状态从持久数据推出)
- [ ] iPad 三态 + 测试 checklist + version.js + PROGRESS 更新

---

## [√] V1-18. 游戏端：成就海报(一次到位，含全亮地图,可长按保存)

**前置**:V1-02(按站算分函数)、V1-17(地图渲染稳定)
**预计时间**:2-3 天(B 区最重的产出,Canvas 合成)
**类型**:游戏端(通关页增强)

> 📌 **一次到位含地图**(v1.9 决定:不做无地图版返工)。竖版可保存图,内容:探险名 + 总分 + 用时 + 答对数 + 各站小结 + 合照拼图 + **全亮地图**(所有点位盖章 + 金名)。
>
> 📌 **复用 V1-02 抽出的「按站算分」函数**(A 区 V1-02 已抽成纯函数,可能在 `quiz-logic.js` 或 `shared/scripts/score-summary.js`)。别重算分。
>
> ⚠️ **性能/体积红线**:合照只读**照片表里已压缩的合影(1280px)**,别加载原图、别再放大;海报合成可能要一两秒,**加「生成中…」提示**别让孩子以为卡了;保存方式和 L2 合影一致——**长按图片保存**(不引 jsPDF、不引截图库,用 Canvas 画完转图片)。

**【复制给 Claude Code 的指令】**

```
在通关庆典页(victory.html)做「成就海报」:一张竖版图,孩子长按可保存到相册。一次到位含全亮地图。

【动手前先确认】
1. 确认 V1-02 抽出的"按站算分"纯函数在哪、入参出参是什么(我要复用,不重算)。
2. 确认通关页能拿到:level(探险名 + 地图配置)、本局 session(总分/用时/各站结果)、
   points(各站名 + mapX/mapY)、照片表里各 L2 合影(已是 1280px 压缩 JPEG)。
3. 确认照片读取用的是压缩过的合影 blob,不是原图。

实现(用 Canvas 画,不引任何 PDF/截图库):
1. 通关页加一个「🎁 生成成就海报」按钮(或进通关页后自动生成,你选体验顺的,告诉我)。
   点击/触发后先显示「⏳ 生成中…」提示,合成完再显示图。
2. 新建一个 Canvas(竖版,比如 1080×1920 这种手机竖屏比例),按从上到下排版画:
   - 顶部:探险名(大标题)
   - 一行汇总:总分 · 用时 · 答对 X/Y 道
   - 各站小结:每站一行(第 N 站 · 站名 · 该站得分),分数复用 V1-02 的按站算分函数
   - 合照拼图:把各 L2 合影缩略平铺(没拍的站留空位或跳过);【只用压缩合影,别加载原图】
   - 全亮地图:把"底图 + 所有点位的圈 + 名字 + 五角星印章(全 completed 态)"画到 canvas 上。
     这部分可以参考/复用 map-overlay 的画法,但 map-overlay 是画 DOM,canvas 要用
     ctx.drawImage(底图) + ctx.arc/ctx.fillText/画五角星 路径自己描。把这段单独写个小函数。
     字体用系统字体,五角星用 canvas 路径画红色五角星。
3. 画完 canvas.toDataURL('image/jpeg', 0.85) 转成图片显示在屏幕中央。
   下方提示「长按图片保存到相册」(和 L2 合影保存体验一致)。
4. 向后兼容:L1 没传地图 → 海报里【不画地图那块】,其余(探险名/分数/各站/合照)照常出。

注意:
- 别碰结算幂等(status/endedAt 只写一次)、别碰撒花、别碰 A 区成绩单/错题本(那是页面里的可交互区,
  海报是另存的一张图,两者独立)。
- 合成耗时大,务必有「生成中…」,且别阻塞页面其他部分。
- 字体系统字体即可,不引字体文件。

写测试清单 docs/test-checklists/V1-18-成就海报.md(按 §5.7 三段结构):
- 打一局(几站、有的拍照有的跳过、有错题)→ 生成海报:探险名/总分/用时/答对数/各站分都对
  (各站分和 A 区通关成绩单一致 ← 验复用了同一个算分函数)
- 全亮地图:所有站都是盖章 + 金名;底图清晰
- 合照拼图:拍了的站有图,没拍的站不报错(留空或跳过)
- 「生成中…」提示出现过;合成不卡死
- 长按图片能唤起 iPad 保存到相册
- L1 没传地图 → 海报无地图块,其余正常
- iPad 三态;Eruda 无红色错误

完成后(按顺序):
1. shared/version.js BUILD_TAG 改 'V1-18-成就海报 · <今天日期>'(任务号 V1-18)
2. 更新 PROGRESS.md(V1-18 完成)
3. 如果本次改动了任何 .js 或 .css 文件,把引用它的 HTML 里对应的 ?v= 版本号一起改成本次任务号;没有 ?v= 引用的文件则忽略此条。
4. 单行英文 commit + push
```

**验证清单**:
- [ ] 竖版海报:探险名 + 总分 + 用时 + 答对数 + 各站小结 + 合照拼图 + 全亮地图
- [ ] 各站分复用 V1-02 算分函数(与通关成绩单一致)
- [ ] 只用压缩合影(1280px)、有「生成中…」、长按保存
- [ ] 无地图 L1 海报省略地图块、其余正常;没破坏结算幂等/撒花
- [ ] iPad 三态 + 测试 checklist + version.js + PROGRESS 更新

---

> 👇 **收尾两块**:数据要带得过去 + 整区验收。

---

## [√] V1-19. 收尾：地图字段进出 JSON 正常 + 放宽导入校验 + schemaVersion 升 1.1

**前置**:V1-09 ~ V1-18 全部完成
**预计时间**:1 天
**类型**:共享层 / 导入导出(数据安全关键)

> ⚠️ **这是 B 区唯一动 schemaVersion 和导入校验的任务,务必三件事一起做、一次做对**(PRD §7.5 + §6.4):
> 1. 确认地图相关字段(Level 的 mapImage/mapFontSize/mapNameColor/mapNameColorCompleted、Point 的 mapX/mapY/questionIntroText/completionText、Level 的 openingStory/endingStory)在导出/导入里**完整带过去**。
> 2. 把导入校验从严格 `=== "1.0"` 放宽为「**主版本号为 1 即接受**」(`schemaVersion.split('.')[0] === '1'`)。
> 3. **同时**把导出盖的章升到 `"1.1"`。
>
> 👉 **顺序很关键**:第 2 步(放宽校验)必须和第 3 步(升章)一起上线。否则一旦先盖了 1.1 章而校验还卡 1.0,新导出的文件自己都导不回来。

**【复制给 Claude Code 的指令】**

```
B 区收尾:确认地图/故事字段进出 JSON 正常,放宽导入校验,把 schemaVersion 升到 1.1。三件事一起做。

【动手前先确认】
1. 找到导出函数(exportFull 之类)和导入校验函数(validateJson / schemaVersion 检查那段,
   PRD §7.5 说现在是严格 === "1.0")。告诉我文件位置和现在校验那几行长什么样。
2. 确认 exportFull 是整对象导出(Level/Point 整个对象都倒)——若是,地图字段会自然跟着出去,
   只需核对;若是挑字段导出,要把地图/故事字段补进去。先告诉我是哪种。

要做的:
1. 字段核对/补齐:导出包含 Level 的 mapImage/mapFontSize/mapNameColor/mapNameColorCompleted/
   openingStory/endingStory,Point 的 mapX/mapY/questionIntroText/completionText。
   导入时这些字段原样写回;老的 1.0 文件【缺这些字段】时,导入按默认值/空处理,不报错。
2. 放宽校验:把 schemaVersion 检查从「严格 === '1.0'」改成「主版本号为 1 即接受」:
   取 schemaVersion.split('.')[0] === '1' 就放行(1.0 和 1.1 都收)。仍要保留"没有 schemaVersion
   字段 → 拒绝"这条。
3. 升章:导出时 envelope 的 schemaVersion 写 "1.1"(原来写 "1.0" 的地方改掉)。
   【第 2 步和第 3 步必须一起改、一起提交】,不能只升章不放宽。

注意:
- 别动 ID、别动导入数据保护(导入前检查未完成游戏 + 二次确认那套,照旧)。
- explanation 字段 MVP 起就在 1.0 文件里,不是 1.1 新增,别误以为要特殊处理。
- assertDbContext 该调还是要调。

写测试清单 docs/test-checklists/V1-19-schema11导入导出.md(按 §5.7 三段结构):
- 编辑端建一个【带地图+坐标+三段文案+开场结局故事】的 L1 → 导出 → 看 JSON 里 schemaVersion 是 "1.1"
  且地图/故事字段都在
- 该 1.1 文件导入游戏端 → 通过校验 → 玩起来地图/故事/盖章都正常
- 【关键】拿一个【旧的 1.0 备份】(B 区之前导出的)→ 导入 → 仍能通过(主版本 1)→ 当无地图 L1 正常玩
- 一个 schemaVersion 是 "2.0" 的假文件 → 被拒(主版本不是 1)
- 没有 schemaVersion 字段的文件 → 被拒
- iPad 上真实 AirDrop 传 1.1 文件导入 → 正常
- iPad 三态;Eruda/F12 无红色错误

完成后(按顺序):
1. shared/version.js BUILD_TAG 改 'V1-19-schema1.1 · <今天日期>'(任务号 V1-19)
2. 更新 PROGRESS.md(V1-19 完成,记一句"schemaVersion 升 1.1 + 放宽校验")
3. 如果本次改动了任何 .js 或 .css 文件,把引用它的 HTML 里对应的 ?v= 版本号一起改成本次任务号;没有 ?v= 引用的文件则忽略此条。
4. 单行英文 commit + push
```

**验证清单**:
- [ ] 地图 + 故事字段导出/导入完整带过去
- [ ] 校验放宽为「主版本号为 1 即接受」(1.0 / 1.1 互通);无 schemaVersion 仍拒
- [ ] 导出盖章升 "1.1",且与放宽校验同一次提交
- [ ] 旧 1.0 备份仍能导入;2.0 假文件被拒
- [ ] iPad 真实 AirDrop 导入 1.1 文件正常
- [ ] 测试 checklist + version.js + PROGRESS 更新

---

## [ ] V1-20. B 区联调 + 真机三态验收

**前置**:V1-09 ~ V1-19 全部完成
**预计时间**:测试半天 + 修补 1-2 天
**类型**:阶段验收

**【你要做的事】**
1. 在编辑端做一个**完整带沉浸感**的探险:上传手绘地图、标好所有 L2 坐标、填开场+结局故事、给几个 L2 填齐三段文案(发现/引导/通关庆祝)、配好字号和两个颜色。
2. 导出 JSON(确认是 1.1),AirDrop 到 iPad 导入。
3. iPad 上完整打一局(故意错几题、用一次求助、有的站拍照有的跳过):
   - [ ] 进探险先看到开场故事(+ 地图全览)→ 开始
   - [ ] 扫码后:发现页内嵌显示发现文案 + 地图圈名浮现 + 答题引导文案 →(同一页点「继续」)进答题
   - [ ] 答完一站:拍照页内嵌显示通关文案 + 地图红五角星盖章 + 名字变金
   - [ ] 答题页/提示页能用 🗺️ 浮动按钮随时看地图,三态显示对
   - [ ] 通关:结局故事 + A 区成绩单/错题本 + **成就海报(含全亮地图,长按能存相册)**
   - [ ] **全程飞行模式跑通**(先联网缓存一次)
4. **续玩验证**:答到一半退出 App,再进来续玩,打开地图已完成的站仍盖章、状态没丢。
5. **向后兼容验证**:拿一个**没传地图、没填故事**的老探险,在新版本游戏端玩一遍——故事页跳过、扫码不弹地图动画、通关海报无地图块,其余全正常。
6. 旧 1.0 备份还能导入(V1-19 已测,这里再过一遍真机)。
7. 发现问题 → 整理后丢给 Claude Code 修。
8. 全部通过 → commit + push → 更新 PROGRESS.md(**B 区完成里程碑**)。

**验证清单**:
- [ ] 完整沉浸流程(故事→发现→浮现→引导→答题→庆祝→盖章→海报)真机走通
- [ ] iPad 飞行模式整局可玩
- [ ] 续玩状态不丢;无地图老探险向后兼容正常
- [ ] 旧 1.0 备份仍可导入
- [ ] PROGRESS.md 标记 B 区完成

> ✅ B 区已完成。C 区(V1-21~25)已展开为下方详细任务。

---

## 更新记录(B 区展开)
- B 区由提纲展开为 V1-09 ~ V1-20 详细任务。三处贯穿约定:① 地图/海报字体先用系统字体;② schemaVersion 升 1.1 + 放宽校验集中在 V1-19 一次做对(开发期间仍盖 1.0);③ V1-14 新建共享 `map-overlay.js`,V1-15/16/17/18 复用。
---

# 🟩 C 区：收尾杂项 + 题型(V1-21 ~ V1-26，详细任务)

> 这是把原提纲展开后的可复制版本。**本区建立在 MVP + A 区 + B 区已有代码上，只增量加，不重写。**
>
> 📌 **三个贯穿全区的约定(每个任务都默认遵守，不再重复写)**：
> 1. **schema 不再动**：`schemaVersion` 已在 V1-19 升到 "1.1" 且校验放宽为「主版本号 = 1 即接受」。C 区新增的字段(题目插图 `image`、session 的 `pausedMs`)都是【附加字段】，跟着整对象导出/导入自然带走，旧文件没有也不报错——**不需要再升 schema**。
> 2. **图片统一压到长边 ≤1600px**：题目插图复用 V1-09 地图上传 / MVP 合影那套 canvas 压缩，别新造。
> 3. **双分支隔离**：C 区都在 `main` 上开发，自动部署到 `/dev`(开发版「寻宝-DEV」)，**孩子玩的 `release` 不受影响**。整个 C 区做完、真机验收通过后，再按 `docs/双分支发布说明.md` 决定是否同步给孩子。

---

## [√] V1-21. 游戏端：主动暂停按钮(暂停计时 + 遮挡屏幕 + 继续)

**前置**：无(C 区第一个，纯游戏端)
**预计时间**：1-1.5 天
**类型**：游戏端

> 📌 **背景**：MVP 故意没做暂停(CLAUDE.md 规则 16：只做自动保存 + 续玩)。现在补一个【真暂停】：孩子中途要休息/吃饭，点暂停 → 计时停住(用时不再涨) + 全屏遮挡(防止盯着题想 / 被剧透) → 点继续接着玩。
> 📌 **用时口径**：现在 `victory.html` 用时 = `session.endedAt - session.startedAt`(约 325 行，纯墙上时间)。本任务加一个累计暂停时长 `pausedMs`，用时改成 `endedAt - startedAt - pausedMs`。`pausedMs` / `pauseStartedAt` 是 session 运行时字段，**不进导出 JSON、不碰 schema**。

**【复制给 Claude Code 的指令】**

```
给游戏端加「主动暂停」按钮。孩子点击 → 计时暂停 + 全屏遮挡 + 「继续游戏」按钮。
动 play/pages/quiz.html、play/pages/hint.html、play/pages/victory.html(用时口径改一处)。
pausedMs / pauseStartedAt 只存 session(IndexedDB)，不进导出、不动 schema。

【动手前先确认】
1. console.log 一个 session，确认有 startedAt/endedAt；pausedMs/pauseStartedAt 现在没有(新加，默认按 0/null)。
2. 确认 victory.html 用时是 session.endedAt - session.startedAt(约 325 行)。
3. 看 quiz.html 顶部条(banner-quiz)：左=题号，右=💡求助(btn-help)；右下角=🗺️地图 FAB。
   暂停按钮要【避开右上(求助)和右下(地图)】→ 放【左上角 / 顶部条最左端】。

1. 暂停按钮(quiz.html 答题屏 + hint.html 找站屏 都加)：
   - 一个【左上角】的小按钮 ⏸(避开求助/地图 FAB)，最小 44×44，半透明深底 + ⏸ 图标。

2. 暂停遮罩(全屏 overlay，两页共用同一套写法)：
   - 盖住整屏，深色半透明 + 居中卡片：「⏸ 已暂停 / 休息一下，准备好再继续吧」+ 大按钮「▶ 继续游戏」。
   - 遮罩出现时下面的题目/选项被挡住即不可点(不用额外禁用逻辑)。

3. 计时逻辑(存进 session)：
   - 点暂停：session.pauseStartedAt = Date.now()，写回 DB；显示遮罩。
   - 点继续：session.pausedMs = (session.pausedMs||0) + (Date.now() - session.pauseStartedAt)；
     session.pauseStartedAt = null；写回 DB；隐藏遮罩。
   - 页面 init 时：若 session.pauseStartedAt 不为 null(上次暂停着就关了 App)→ 直接显示遮罩，
     点继续再累加。这样关 App 期间也算暂停、不计时。

4. 用时口径(victory.html)：
   - elapsed 改成 session.endedAt - session.startedAt - (session.pausedMs || 0)，再 Math.max(0,…) 兜底。
     结算幂等(status/endedAt 只写一次)不动。

5. 容错：老 session 没有 pausedMs/pauseStartedAt → 当 0 / 未暂停，不报错。
6. 不碰：求助逻辑、地图 FAB、结算幂等、撒花、答题判分。

写测试清单 docs/test-checklists/V1-21-暂停按钮.md(按 §5.7 三段)：
- 答题中点暂停 → 全屏遮罩 + 题目被挡 → 点继续回到原题，进度/求助次数不变
- 暂停约 30 秒再继续 → 通关页用时【没有】把这 30 秒算进去
- hint 找站屏点暂停 → 同样遮挡 + 继续
- 暂停状态下关 App，重开续玩 → 直接回到暂停遮罩(没偷偷计时)，继续可玩
- 老存档(无 pausedMs)→ 通关页用时正常、不报错
- iPad 三态；Eruda 无红色错误

完成后(按顺序)：
1. shared/version.js BUILD_TAG 改 'V1-21-暂停按钮 · <今天日期>'(PowerShell Get-Date 取；任务号 V1-21)
2. service-worker.js 升 CACHE_VERSION(改了 quiz/hint/victory 预缓存页)
3. 更新 PROGRESS.md(V1-21 完成)
4. 改了 .js/.css 的话，引用它的 HTML 的 ?v= 同步改本任务号；没有则忽略
5. 单行英文 commit + push
```

**验证清单**：
- [ ] quiz + hint 左上角暂停按钮(避开求助/地图)
- [ ] 暂停 = 全屏遮挡 + 继续按钮；继续回原处，状态不变
- [ ] pausedMs 累计、用时扣掉暂停时长；pauseStartedAt 持久化(关 App 续暂停)
- [ ] 老存档不报错；不碰求助/结算/撒花
- [ ] SW 版本升 + 测试 checklist + version.js + PROGRESS 更新

---

## [⏸ 暂缓] V1-22. 题目插图(L3 加可选配图，base64 进 JSON)

> ⏸ **2026-06-23 决定暂缓**：目前题目都用 AI 集中生成、不需要截图配图，V1-22 跳过(任务保留，以后想做再捡)。**直接做 V1-23**。另：用户更想要【填空题】——原在 D 区 V1-32(多选/判断/填空)，考虑提前单独拉出来做，**做完 V1-23 再专门规划该任务**。

**前置**：无
**预计时间**：1-1.5 天
**类型**：编辑端 + 游戏端

> 📌 给题目加一张【可选】配图(识物、找不同、看图选字…)。图存 base64 在 Question 对象的新字段 `image`(默认 null)，跟着整对象导出/导入自然带走，旧题没有该字段不报错——**不动 schema**。上传压到长边 ≤1600px。CSV 导入不带图(base64 不适合塞 CSV)，配图只在编辑端表单里加。

**【复制给 Claude Code 的指令】**

```
给题目(L3)加一个【可选配图】字段 image(base64)。编辑端能传/预览/移除，游戏端答题时显示在题干上方。
压到长边 ≤1600px。不动 schema(附加字段，自然随导出走)。

【动手前先确认】
1. 找到编辑端题目(L3)的新建/编辑表单文件，以及保存题目时把表单值写进 Question 的代码。告诉我位置。
2. console.log 一个 Question，确认【目前没有】image 字段(本任务新增；默认 null)；确认它会被
   exportFull 整对象导出(应该和 explanation 一样)。
3. 找到 play/pages/quiz.html 显示题干(#question-text)的地方。

编辑端(题目表单)：
1. 加「配图(可选)」：<input type="file" accept="image/*">。
2. 选图后 canvas 压缩:长边 ≤1600px、JPEG quality 0.8、转 base64 存进 question.image。
   压缩写法复用 V1-09 地图上传那段(或 MVP 合影压缩)，别新造。
3. 已选图显示缩略图预览 + 「移除配图」按钮(image 设回 null)。
4. 保存题目时写回 image(没传就 null)。不动 CSV 导入、不动 schema、不动导出逻辑。

游戏端(quiz.html)：
1. 渲染题目时若 question.image 非空：题干上方显示 <img>，宽度自适应、限制最大高度
   (别把选项挤出屏幕，如 max-height:40vh; object-fit:contain)。
2. image 为空 → 不显示图、不留空块(向后兼容老题)。
3. 别影响拼音(V1-05)：图只是题干上方多一张图，题干/选项渲染逻辑不变。

写测试清单 docs/test-checklists/V1-22-题目插图.md(按 §5.7 三段)：
- 编辑端给一道题传大图(如 3000px)→ 保存重开缩略图在，F12 看 image 是压缩后 base64(≤1600)
- 移除配图 → image 变 null
- 不传图的老题 → 不报错、游戏端不显示图
- 游戏端：有图的题，图显示在题干上方，选项不被挤出屏幕；开拼音也正常
- 导出含图的题 → JSON 里带 image；再导入回来图还在(整对象走，无需改 schema)
- iPad 三态；F12/Eruda 无红色错误

完成后(按顺序)：
1. shared/version.js BUILD_TAG 改 'V1-22-题目插图 · <今天日期>'(任务号 V1-22)
2. service-worker.js 升 CACHE_VERSION(改了 quiz.html)
3. 更新 PROGRESS.md(V1-22 完成)
4. 改了 .js/.css 的话 HTML 的 ?v= 同步改；没有则忽略
5. 单行英文 commit + push
```

**验证清单**：
- [ ] 编辑端题目表单可传/预览/移除配图，压到 ≤1600px 存 image
- [ ] 游戏端有图显示在题干上方、不挤出选项；无图不显示
- [ ] 含图题导出/导入图还在(不动 schema)
- [ ] 老题不报错；不影响 CSV/拼音
- [ ] SW 升 + 测试 checklist + version.js + PROGRESS 更新

---

## [√] V1-23. 单 L1 导出(编辑端) + 单 L1 追加导入(冲突走覆盖确认)

**前置**：无(建议放本区最后，导入导出是收尾)
**预计时间**：2 天
**类型**：编辑端 + 游戏端 + 共享层(json-io.js)

> 📌 现在只有【整库】导出/导入(`exportType="full"`，清空全部再写)。本任务加【单个探险】导出 + 【追加】导入：导出一个 L1 + 它的点位 + 这些点位引用的题目，`exportType="single-level"`；导入时**不清空别的探险**，只把这一个 L1 的子树 upsert 进去。
> 📌 **冲突简化(已拍板)**：撞同 ID(同一个 L1 已存在)→ 弹「这个探险已存在，覆盖它?」确认 → 整份覆盖该 L1 子树(点位 + 引用题目按 ID upsert)，**不做逐题差异分析 / 合并统计**。
> 📌 **schema 不升**：仍 "1.1"(V1-19 已放宽校验到主版本=1)。single-level 文件就是 1.1。`validateJson` 要**额外接受** `exportType="single-level"`，full 维持不变。
> ⚠️ **共享题目副作用**：单 L1 导入会按 ID 覆盖它引用的题目，若这些题也被别的探险用着，会被导入版本覆盖(简化方案接受这点，但要在确认弹窗里给孩子家长一句小字提醒)。

**【复制给 Claude Code 的指令】**

```
加「单个探险」导出 + 追加导入。不破坏现有整库 full 导出/导入。
动 shared/utils/json-io.js、编辑端 admin(L1 详情页 + import-export 页)、游戏端 play/pages/settings.html。

【动手前先确认】
1. 读 shared/utils/json-io.js：现有 exportFull / validateJson / importFull + SCHEMA_VERSION。
   告诉我现在 validateJson 怎么校验 exportType(应该只认 'full')。
2. 找到编辑端 L1 详情页文件(V1-09/10 改过的那个) + 整库导入页 import-export.html。
3. 找到游戏端 settings.html 里导入那段(import validateJson/importFull)。

第 1 步 —— shared/utils/json-io.js 加两个函数 + 放宽校验：
A. exportSingleLevel(db, levelId) → 返回 JSON 字符串：
   取这一个 level；它的 points(where levelId)；这些 points 的 questionIds 并集对应的 questions。
   envelope = { schemaVersion: SCHEMA_VERSION, exportedAt, exportType:'single-level',
     data:{ levels:[level], points:[...], questions:[...] } }。形状和 full 一样，只装一个 L1 子树。
B. validateJson 放宽：现在只认 exportType==='full'，改成【接受 'full' 或 'single-level'】，
   其余校验(schemaVersion 主版本=1、data 三数组存在)不变。返回值加上 exportType
   (原来 {ok,data} → {ok,data,exportType}，老调用方只读 .ok/.data 不受影响)。
C. importSingleLevel(db, data) → 追加写(不清空)：
   事务里 db.levels.put(data.levels[0])；data.points 逐个 put；data.questions bulkPut。
   全程 upsert(有则覆盖、无则新增)，【不 clear】。打开 Dexie 前照常 assertDbContext。

第 2 步 —— 编辑端单 L1 导出：
   L1 详情页加「📤 导出这个探险」按钮 → 调 exportSingleLevel → 下载文件名带探险名的 .json。

第 3 步 —— 追加导入(编辑端 import-export + 游戏端 settings 都支持)：
   选文件 → validateJson：
   - exportType==='full' → 走原来的整库覆盖流程(不变)。
   - exportType==='single-level' → 追加导入：
     * 该 level.id 已存在 → 二次确认「探险《名字》已存在，覆盖它(连同点位和题目)吗?」→ 确认才写。
     * 不存在 → 给「将添加探险《名字》」确认后写。
     * ⚠️ 写前检查：该 levelId 若有 in_progress 的 session → 多提示一句「这个探险有没玩完的存档，
       覆盖会影响续玩，确定继续?」(整库导入已有未完成游戏检查，这里对单 L1 同理)。
   - 成功后提示「已导入探险《名字》：X 个点位、Y 道题」。

第 4 步 —— 共享题目副作用提示：
   追加导入确认弹窗加小字:「注意:它引用的题目会按导入版本更新，可能影响其它用到这些题的探险。」

不做(明确)：逐题差异/合并统计、单题导出、把 full 改成 upsert。schema 不升(仍 1.1)。

写测试清单 docs/test-checklists/V1-23-单L1导入导出.md(编辑端桌面+iPad Safari；游戏端按 §5.7 三段)：
- 编辑端导出某 L1 → 得到 single-level json(F12 看 exportType、data.levels 只 1 个)
- 把它导入【另一套数据】(游戏端)→ 该探险被加进去，原有其它探险【还在】(没被清空)
- 再导入一次(同 ID)→ 弹「已存在，覆盖?」→ 确认后该探险更新，别的不动
- 该探险有未完成存档时导入 → 弹续玩影响提示
- 旧的【整库 full】文件导入 → 仍是整库覆盖(老流程没坏)
- 旧 1.0 整库文件导入 → 仍能导入(V1-19 放宽校验没被破坏)
- F12/Eruda 无红色错误；iPad 三态(游戏端导入)

完成后(按顺序)：
1. shared/version.js BUILD_TAG 改 'V1-23-单L1导入导出 · <今天日期>'(任务号 V1-23)
2. 改了 settings.html 等预缓存页 → service-worker.js 升 CACHE_VERSION
3. 更新 PROGRESS.md(V1-23 完成)
4. HTML 的 ?v= 按需同步；没有则忽略
5. 单行英文 commit + push
```

**验证清单**：
- [ ] exportSingleLevel：导出单 L1 + 点位 + 引用题目，exportType='single-level'
- [ ] validateJson 接受 full 和 single-level(返回带 exportType)；旧 full / 1.0 不受影响
- [ ] importSingleLevel：upsert 追加，不清空其它探险
- [ ] 撞同 ID → 覆盖确认；有未完成存档 → 续玩影响提示
- [ ] 编辑端单 L1 导出按钮；共享题目覆盖副作用有小字提示
- [ ] 不升 schema；SW 升 + 测试 checklist + version.js + PROGRESS 更新

---

## [ ] V1-24. 填空题（L3 新题型，编辑端 + 游戏端）

**前置**：V1-23
**预计时间**：1-1.5 天
**类型**：编辑端 + 游戏端

> 📌 **背景**：原在 D 区 V1-32（多题型增强）的填空部分，提前单独拉出来做，因为它是完整独立的功能，适合作为 C 区收尾的一部分。
> 📌 **模型**：Question 加 `fillAnswers: string[]`（可接受的多个答案），`type: 'fill'`。`options`/`correctAnswer` 填空题不用、存空即可。答案匹配：**去前后空格、不区分大小写**。
> 📌 **不升 schema**：`fillAnswers` 是附加字段，整对象导出/导入自然带走，旧题没有不报错。

**【复制给 Claude Code 的指令】**

```
给题库加填空题（type: 'fill'）。编辑端能出题，游戏端能答题。
动 admin/pages/questions.html、admin/scripts/question-form.js、play/pages/quiz.html、
play/service-worker.js、shared/version.js。

【模型约定】
- Question 新字段 fillAnswers: string[]（可接受答案，可多个）
- type: 'fill'；options=[]; correctAnswer=''（不用，存空）
- 答案匹配：userInput.trim().toLowerCase() === any fillAnswers[i].trim().toLowerCase()
- 不动 schema（附加字段，导出/导入自然带）

【编辑端 admin/pages/questions.html】
1. 启用填空题按钮：把 disabled 的「填空题【V1】」改成可用 data-type="fill"：
   <button class="q-type-btn" data-type="fill">填空题</button>
2. 在「正确答案」section 下面加「填空答案」区块：
   <div id="fill-answers-section" class="detail-section" style="display:none">
     <div class="detail-section-title">填空答案（可填多个可接受的答案）</div>
     <p style="font-size:13px;color:var(--color-text-muted);margin-bottom:8px">
       游戏里去除空格、不区分大小写匹配。填多个可接受写法（如「北京」「北京市」都行）。
     </p>
     <div id="fill-answers-container"><!-- JS 填入 --></div>
     <button id="btn-add-fill-answer" class="btn btn-secondary btn-sm" style="margin-top:8px">
       ＋ 添加可接受答案
     </button>
   </div>

【编辑端 admin/scripts/question-form.js】
A. 加模块级变量：let currentFillAnswers = [''];
B. 加辅助函数 showFillMode() / showChoiceMode()：
   showFillMode：隐藏 #options-container 的父 .detail-section、#correct-answer-group 的父 .detail-section；显示 #fill-answers-section
   showChoiceMode：反过来
C. switchType('fill')：
   - showFillMode()
   - currentFillAnswers = ['']；renderFillAnswers()
   switchType 切回 single_choice/true_false：showChoiceMode()，原有逻辑不变
D. renderFillAnswers()：在 #fill-answers-container 渲染每行 input + 删除按钮（第 0 行不能删）
E. initQuestionForm() 里绑定 #btn-add-fill-answer 的 click
F. showQuestionForm(questionId)：
   - 顶部 reset 加 currentFillAnswers = ['']
   - 编辑已有 fill 题时读 q.fillAnswers || ['']
   - 末尾 renderOptions/renderAnswerBtns/updateAddOptionBtn 改成：
     if (currentType==='fill') { showFillMode(); renderFillAnswers(); }
     else { showChoiceMode(); renderOptions(); renderAnswerBtns(); updateAddOptionBtn(); }
G. saveQuestion()：
   - fill 类型：只验 text+subject+fillAnswers≥1；data.options=[] data.correctAnswer='' data.fillAnswers=trimmed
   - 非 fill：维持原有验证

【游戏端 play/pages/quiz.html】
在 showQuizScreen() 选项渲染那段加分支：
- if (question.type === 'fill') → grid.style.display='block'; renderFillInput()
- else → grid.style.display=''; 原有选项按钮渲染

新增 renderFillInput()：清空 grid，放 input#fill-input + button#btn-fill-submit；
   Enter 键触发提交；requestAnimationFrame 聚焦 input

新增 checkFillAnswer(question, userInput)：
   answers=question.fillAnswers||[]；
   return answers.some(a => a.trim().toLowerCase() === userInput.trim().toLowerCase())

新增 onSubmitFill()：
   optionsLocked 守门；取 fill-input.value；
   答对 → playSound/recordQuestionResult/showFeedback('correct')/1s 后切题
   答错：
   - wrongChoices.push(userInput)；attemptCount++；playSound(sfxWrong)；input 抖动（option-shake class）
   - 第 1-2 次：showFeedback('wrong',{hint})；0.6s 后恢复 input/submit 可用、清空 input
   - 第 3 次：保底，recordQuestionResult score=0，showFeedback('bust', {correctText:fillAnswers[0], explanation, onConfirm})

在 help-confirm 的两处「恢复选项」代码处（btnYes setTimeout + btnNo click），追加：
   const fi=document.getElementById('fill-input'); const fs=document.getElementById('btn-fill-submit');
   if(fi){fi.disabled=false;fi.focus();} if(fs) fs.disabled=false;

向后兼容：question.type 不是 'fill' 时走原有选项渲染路径（包括 true_false/single_choice/undefined）

【SW + version】
- service-worker.js：CACHE_VERSION → 'treasure-hunt-runtime-v2.25'
- shared/version.js：BUILD_TAG = 'V1-24-填空题 · 2026-06-23'

写测试清单 docs/test-checklists/V1-24-填空题.md（按 §5.7 三段结构）：
- 编辑端：新建填空题 → 答案区出现；填多个可接受答案 → 保存 → 重开仍在；切回选择题 → 恢复正常
- 游戏端：填空题显示文本框不显示选项按钮；输正确答案（大小写/前后空格变体）→ 答对
  答错 → 抖动 + 提示；错满 3 次 → 保底通关 + 显示正确答案
- 求助按钮：fill 题也能用，用了求助得 3 分
- 成绩单（V1-02）：fill 错题 wrongChoices 显示输入的文字，正确答案显示 fillAnswers[0]
- 旧选择题/判断题不受影响；不升 schema

完成后(按顺序):
1. shared/version.js BUILD_TAG 改 'V1-24-填空题 · 2026-06-23'
2. service-worker.js CACHE_VERSION 改 'treasure-hunt-runtime-v2.25'
3. 更新 PROGRESS.md（V1-24 完成）
4. 单行英文 commit + push
```

**验证清单**：
- [ ] 编辑端填空题可创建（fillAnswers 数组，至少 1 个）；切回选择题正常
- [ ] 游戏端填空题显示文本框，不显示选项按钮
- [ ] 答对/答错/保底/求助/计分逻辑与选择题一致
- [ ] 大小写+前后空格不影响判对
- [ ] 旧选择题/判断题不受影响；不升 schema
- [ ] SW 升 + 测试 checklist + version.js + PROGRESS 更新

---

## [√] V1-25. 编辑端：题库随机抽题（按现有筛选随机抽，避开已用 ≥2 次）

**前置**：V1-24
**预计时间**：0.5-1 天
**类型**：编辑端（纯加在现有「选题面板」里，不动数据模型）

> 📌 **背景**：原 D 区 V1-34 的「标签随机抽题」提前到 C 区。给 L2 选题时，除了手动逐题勾选，再加一个「随机抽 N 道」按钮，省得家长一道道翻题库。
> 📌 **「标签」= 题库筛选条的四个维度**：数据模型里**没有独立 tags 字段**，新增字段属大改动，不做。所以「按标签随机」落地为**复用题库筛选条已有的「学科 + 难度 + 年龄 + 使用次数」+ 题干搜索**——家长先选好筛选，再点随机抽取，从符合条件的题里抽。其中「年龄」「使用次数」选题面板原本没有，本任务补上（与题库主页同一套）。
> 📌 **使用次数 = 实时引用数**：`usedCount` 字段已遗留（永远是 0，从不维护，见 CLAUDE.md 规则 14）。本任务在选题面板按「扫所有点位 questionIds」实时算引用数，用于「使用次数」筛选 + 显示「已用 N 次」标签 + 随机抽题的软避让，三处口径与题库主页一致。
> 📌 **「避开已用 ≥2 次」= 软避让**：优先抽实时引用数 < 2 的题；不够 N 道时才补抽 ≥2 次的并提示。**不硬性排除**，避免题库小的时候抽不出来。（家长也可直接用「使用次数」筛选硬性限定范围。）
> 📌 **纯编辑端**：admin 不是 PWA、无 Service Worker，**不需要升 CACHE_VERSION**。

**【复制给 Claude Code 的指令】**

```
给编辑端 L2「选题面板」(question-picker-modal) 加「随机抽 N 道」功能。
复用面板现有的学科/难度/搜索筛选当作抽题范围，优先抽 usedCount<2 的题。
动 admin/pages/levels.html(选题面板 HTML) + admin/scripts/point-form.js(抽题逻辑)。
不新增数据字段、不动 schema、不动游戏端、不升 SW。

【动手前先确认】
1. 读 admin/scripts/point-form.js：选题面板 renderPickerList() 怎么算 filtered
   (matchesQuestionFilter + 排除已绑定 bound + 排除同探险占用 pickerOccupiedIds)，
   以及 pickerSelectedIds(Set) 怎么驱动勾选 + updatePickerCount()。先告诉我。
2. 确认 question 有 usedCount(实时引用数，openPicker 里按 usedCount 排序加载过)。

第 1 步 —— levels.html 选题面板加抽题控件：
   在 picker-filters 那行下面、picker-question-list 上面，加一行随机抽题条：
   - 一个数字输入框 id="picker-random-count" 默认 3，min 1 max 10，窄一点。
   - 一个按钮 id="btn-picker-random"「🎲 随机抽取」。
   - 旁边一个 span id="picker-random-hint" 放结果提示(默认空)。
   样式跟现有 picker-filters 一致即可，别新造一套。

第 2 步 —— point-form.js 实现 drawRandomQuestions():
   A. initPointForm() 里绑定 #btn-picker-random 的 click → drawRandomQuestions()。
   B. drawRandomQuestions():
      1. 读 N = clamp(parseInt(picker-random-count), 1, 10)。
      2. 算候选池 pool：和 renderPickerList 同一套过滤
         (matchesQuestionFilter(当前筛选) && !bound && !occupied)，
         再【额外排除已在 pickerSelectedIds 里的】(避免重复抽到已勾的)。
      3. pool 空 → picker-random-hint 提示「没有可抽的题(调整筛选或先清空已选)」，return。
      4. 分两组：preferred = usedCount<2，fallback = usedCount>=2。
      5. 各自 Fisher-Yates 洗牌；先从 preferred 取，不够再从 fallback 补，共取 min(N, pool.length)。
      6. 取到的 id 逐个 pickerSelectedIds.add(id)；renderPickerList()(会自动恢复勾选)；updatePickerCount()。
      7. 结果提示 picker-random-hint：
         - 正常：「已随机选中 X 道(已加入勾选，可再手动调整)」
         - 用到了 ≥2 次的题：补一句「其中 Y 道是已用较多的题」
         - 没凑够 N：「只抽到 X 道(符合条件的题不够)」
   C. 写一个小工具 shuffle(arr) 用 Fisher-Yates，别引库。
   D. 抽完【不自动确认】——家长仍要点「确认添加」，保留人工复核。

第 3 步 —— 边界处理：
   - 关闭/重开面板 openPicker() 时清空 picker-random-hint 和把 count 复位 3。
   - 抽题只改 pickerSelectedIds，不直接动 currentQuestionIds(确认添加时才合并，沿用现有逻辑)。

写测试清单 docs/test-checklists/V1-25-随机抽题.md(编辑端：桌面 Chrome + iPad Safari 两段)：
- 选「数学+★」筛选 → 随机抽 3 → 勾选里多出 3 道数学简单题，计数 +3，可手动再增减
- 抽到的题点「确认添加」→ 进绑定列表；没点确认就关面板 → 不写入
- 题库符合条件只有 2 道却抽 5 → 只抽到 2 + 提示「不够」
- 全是已用≥2次的题 → 仍能抽到 + 提示「已用较多」
- 连点两次随机抽取 → 第二次不会重复抽到第一次已勾的(从池里排除已选)
- 抽题 + 手动勾选混用 → 计数正确，确认添加都进列表
- F12 Console 无红色错误

完成后(按顺序)：
1. shared/version.js BUILD_TAG 改 'V1-25-随机抽题 · 2026-06-23'(任务号 V1-25)
2. 更新 PROGRESS.md(V1-25 完成)。admin 无 SW，不升 CACHE_VERSION。
3. 改了 point-form.js → 若 levels.html 用 ?v= 引用它则同步改;没有则忽略。
4. 单行英文 commit + push
```

**验证清单**：
- [ ] 选题面板加「🎲 随机抽 N 道」按钮，复用现有学科/难度/搜索筛选当范围
- [ ] 优先抽 usedCount<2，不够再补 ≥2 次并提示（软避让，不硬排除）
- [ ] 抽到的加入 pickerSelectedIds（自动勾选），家长仍需手动「确认添加」
- [ ] 连抽不重复、池空/不够/全是高频题都有提示
- [ ] 不新增字段、不动 schema、不动游戏端、不升 SW
- [ ] 测试 checklist + version.js + PROGRESS 更新

---

## [ ] V1-26. C 区联调 + 真机三态验收

**前置**：V1-21 ~ V1-25 全部完成
**预计时间**：测试半天 + 修补 1 天
**类型**：阶段验收

**【你要做的事】**
1. 编辑端：给几道题出填空题（含多个可接受答案）；用「随机抽题」给某 L2 配题；单独导出该探险。
2. iPad 导入该 single-level 文件(验证不清空别的探险)，完整打一局：
   - [ ] 答题中暂停 → 遮挡 + 继续；用时不含暂停时长
   - [ ] 暂停着关 App → 续玩回到暂停态
   - [ ] 填空题正常出现文本框；答案变体（大小写/空格）都判对
   - [ ] 全程飞行模式跑通(先联网缓存一次)
3. 单 L1 追加导入再测「同 ID 覆盖确认」「有存档时的续玩提示」。
4. 编辑端回归：随机抽题不破坏手动选题；删除引用保护、CSV 导入仍正常。
5. 回归：续玩 / A 区成绩单错题本 / B 区地图故事海报 都没被 C 区破坏。
6. 全部通过 → commit + push → 更新 PROGRESS.md(**C 区完成里程碑**)。
7. 决定要不要把 C 区(连同 A/B)正式发布给孩子：按 `docs/双分支发布说明.md` 把 main 同步到 release。

**验证清单**：
- [ ] V1-21~V1-25 在 iPad 三态 / 桌面全通
- [ ] 暂停计时、填空题、随机抽题、单 L1 追加导入都正确
- [ ] A/B 区功能回归无破坏
- [ ] PROGRESS.md 标记 C 区完成
- [ ] (可选)已按双分支说明决定是否发版给孩子

> ✅ C 区做完后，玩过 A/B/C，再决定 D 区(多人)做不做。

---

## 更新记录(C 区展开)
- C 区由提纲展开为 V1-21 ~ V1-26 详细任务：① V1-21 主动暂停按钮(pausedMs 扣时长，运行时字段不进导出)；② V1-22 题目插图(暂缓)；③ V1-23 单 L1 导出 + 追加导入(exportType='single-level'，撞 ID 走覆盖确认)；④ V1-24 填空题(type='fill'，fillAnswers数组，编辑端+游戏端，不升 schema)；⑤ V1-25 题库随机抽题(复用现有筛选当范围，优先抽 usedCount<2，纯编辑端不升 SW)；⑥ V1-26 联调验收。**题型工作(判断/填空/随机抽题)从原 D 区前移到 C 区，D 区只剩多人。** 全区不升 schema(V1-19 已到 1.1 且放宽)。C 区在 main 开发不影响孩子 release，详见 `docs/双分支发布说明.md`。

---

# 🟥 D 区：多人（合作式轻量版，V1-27 ~ V1-31，详细任务）

> ⚠️ **重大简化（2026-06-24 与用户确认）**：原 PRD 的「个人积分归属 / 难度加成 / 连续答对计数 / 答题之星 / 投票颁奖 / 保底奖」**全部推到 V2**。V1 的多人是**合作式轻量版**：
> - 开局创建玩家（头像/名字/编号/颜色）+ 队名，纯粹增加仪式感和合影归属感；
> - **答题流程和单人完全一样**——还是全队共享一个总分，**不选「谁答对」、不算个人分**；
> - 通关时给**整支队伍**颁一枚奖（奖名 + 奖图由每个 L1 自己配置，和主题呼应），人人有份，所以**不需要保底奖**。
>
> 📌 **三个贯穿全区的约定（每个任务都默认遵守）**：
> 1. **单人玩法零改动**：不创建玩家（人数保持 1）就和现在一模一样，多人那套 UI/数据都不出现。
> 2. **全部是追加字段，不升 schema**：B 区 V1-19 已到 "1.1" + 放宽校验（主版本号为 1 即接受），新字段自然跟着 exportFull/importFull 倒进倒出，不动 schemaVersion。
> 3. **图片统一 base64 + canvas 压缩**：头像压到长边 ≤1280（复用合影那套），奖图是小图标压到长边 ≤800。
>
> 📌 **数据落点（实现时照此，字段不存在就先告诉用户再加）**：
> - `Level`：新增 `groupAwardName`（默认 `""`）、`groupAwardImage`（默认 `null`，base64）。**`customAwards` 留给 V2 投票，本区不碰**。
> - `GameSession`：真正启用已预留的 `players`（数组）、`teamName`（字符串）。`voteRecords` / `awards` 留给 V2。
> - 每个 player 形如 `{ id:"p_xxx", name:"小明", playerNumber:"3", color:"#FF6B6B", avatarPhotoId:"ph_xxx"|null }`。
> - 头像存 `photos` 表，`type:"avatar"`，`ownerId` = player 的 id（数据模型早预留）。

---

## [ ] V1-27. 游戏端：开局多人创建（人数 + 头像/名字/编号/颜色 + 队名）

**前置**：C 区完成（D 区第一个任务）
**预计时间**：2-3 天（D 区最费交互的一块）
**类型**：游戏端

> 📌 玩家创建插在「点开探险 → 开始冒险」之间（PRD §4.2.2：人数选择 → 玩家创建 → 队名 → 开始）。**头像拍摄可选**；没拍头像就用「选中的颜色块 + 编号数字」当头像。**单人（人数=1）维持现状**，不进多人创建界面。

**【复制给 Claude Code 的指令】**

```
给游戏端开局加「多人创建」。插在 start-level.html 里，点「开始冒险」之后、创建 GameSession 之前。
单人（人数保持 1）维持现状不变。动 play/pages/start-level.html（主要）、play/service-worker.js。
头像照片存 photos 表。

【动手前先确认】
1. 读 start-level.html：现在点「开始冒险!」的两阶段逻辑（有 openingStory 先切故事视图，再点一次才
   建 session，约 132-180 行）。多人创建要插在【openingStory 之前】，顺序变成：
   开始冒险 → [人数≥2 时] 玩家创建视图 → [有 openingStory] 故事视图 → 建 session 跳 hint。
2. console.log 一个现有 session：确认 GameSession 上 players / teamName 字段（PRD §6.5 已预留，
   MVP 没写值）。photos 表确认有 type 字段（'avatar' | 'completion_photo'）。
3. 找游戏端现成的「拍照 + canvas 压缩」写法（photo.html，长边 1280、JPEG 0.7、写 photos 表），头像复用同一套。

第 1 步（人数选择，最小侵入单人）：
- 在探险概览页（level-overview）「开始冒险!」按钮上方，加一行「👥 几个人一起玩？」+ 一个 1~6 的选择
  （6 个小圆按钮或下拉，默认 1）。
- 默认 1 时，点「开始冒险!」流程和现在【完全一样】，不弹任何多人界面（满足单人零改动）。
- 选了 ≥2，点「开始冒险!」先进玩家创建视图（参照 story-section 那样在本页切视图，别新建页面）。

第 2 步（玩家创建视图，N = 人数）：
- 顶部一个「队名（可选）」文本框，小字「不填就叫『我们队』」。
- N 张玩家卡片，每张含：
  * 头像区：「📷 拍头像（可选）」按钮 → 调系统相机（用 <input type="file" accept="image/*" capture>
    最省事，别上 getUserMedia），拍完 canvas 压到长边 ≤1280 存 base64 预览；可「重拍/删除」。
  * 名字：文本框（可空，空就显示「玩家N」）。
  * 编号：数字输入框（type="number"，1 位即可），默认填该卡序号（1/2/3…），允许家长改。
  * 颜色：6 个常见颜色色块单选（给定一组好看的：红 #FF6B6B、橙 #F5A623、黄 #FbD437、绿 #4CAF50、
    蓝 #4A90E2、紫 #9B59B6），默认按序号分配不同色。
  * 头像 fallback 预览：没拍头像时，头像区显示一个圆——背景=选中颜色、中间=编号数字（白字），
    选色/改编号要实时更新这个圆。
- 底部「🚀 开始冒险！」确认按钮。

第 3 步（保存进 session）：
- 确认时，对每个【拍了头像】的玩家：写一条 photos 记录（type:'avatar'、ownerId = 该 player 的 id、
  压缩后的 blob/base64，沿用 photo.html 的写法），拿回 avatarPhotoId。
- 组装 players 数组：[{ id:generateId('p'), name, playerNumber:String(编号), color, avatarPhotoId:ph_xxx|null }, …]。
- 建 GameSession 时（约 159 行那个对象）补两个字段：players、teamName（队名空就存 '我们队'）。
- 单人（人数=1，没进创建视图）：players 存 []、teamName 存 ''（和老存档一致，victory 端按空处理）。
- 其余建 session、跳转逻辑不变。

第 4 步：
- service-worker.js：CACHE_VERSION 在当前 'treasure-hunt-runtime-v2.26' 基础上 +0.01 → v2.27。
  （改了 start-level.html 这个预缓存页，必须升。）

注意：写最简单的代码、多写中文注释、函数 20-30 行内；别动答题/计分（那是 V2）；别破坏 openingStory 两阶段流程。

写测试清单 docs/test-checklists/V1-27-多人创建.md（按 §5.7 三段结构）：
- 人数保持 1 → 点开始流程和以前一模一样，不出现玩家创建（单人零改动）
- 选 3 人 → 出现 3 张卡 + 队名框；填名字、改编号、选颜色 → 没拍头像的卡显示「颜色块+编号」圆，改色/改号实时变
- 给其中 1 人拍头像 → 预览正常、可重拍/删除；确认后 photos 表多一条 type:'avatar'
- 确认开始 → console.log session：players 有 3 条（结构对）、teamName 对；avatarPhotoId 有头像的非空、没头像的为 null
- 有 openingStory 的探险：玩家创建 → 故事页 → 开始，顺序对，故事没被破坏
- 飞行模式（先联网缓存过 v2.27）创建多人正常
- iPad 三态；拍头像真机走一遍（相机权限/重拍）

完成后（按顺序）：
1. shared/version.js 的 BUILD_TAG 改 'V1-27-多人创建 · <今天日期>'（PowerShell Get-Date -Format 'yyyy-MM-dd'）
2. 更新 PROGRESS.md（V1-27 完成）
3. 改了 .js 的话把引用它的 HTML 里 ?v= 版本号同步改成本任务号
4. 单行英文 commit + push（带上 start-level.html / service-worker.js / version.js / PROGRESS.md）
```

**验证清单**：
- [ ] 人数=1 单人流程零改动；≥2 才进玩家创建
- [ ] 每人：头像可选 + 名字 + 数字编号 + 6 色选一；无头像显「颜色块+编号」圆且实时更新
- [ ] 队名可选（空=「我们队」）
- [ ] players/teamName 正确写进 GameSession；头像进 photos 表 type:'avatar'
- [ ] openingStory 流程未被破坏；SW 升 v2.27
- [ ] 测试 checklist + version.js + PROGRESS 更新

---

## [ ] V1-28. 编辑端：L1 通关小组奖配置（奖名 + 奖图，可空）

**前置**：无（可与 V1-27 独立，但排在这）
**预计时间**：0.5-1 天
**类型**：编辑端（纯加表单字段）

> 📌 每个 L1 配一枚「通关小组奖」，奖名 + 一张奖杯/奖牌图片，和探险主题呼应。**柔性可空**：没配就用 App 内置默认奖（V1-29 兜底）。复用 V1-09 的图片压缩思路，奖图是小图标，压到长边 ≤800 就够。

**【复制给 Claude Code 的指令】**

```
给编辑端 L1 表单加「通关小组奖（可选）」。每个 L1 配一个奖名 + 一张奖图。
不新增到 customAwards（那留给 V2 投票），用两个新字段；不动 schema、不动导出逻辑。

【动手前先确认】
1. 找到 V1-09 改过的同一个 L1 表单文件（应是 admin/pages/levels.html + admin/scripts/level-form.js，
   地图上传字段就在那），告诉我地图字段那一段在哪。
2. console.log 一个 Level：确认【没有】 groupAwardName / groupAwardImage 这两个字段——确实没有的话，
   在新建/保存 L1 的地方给它们默认值（"" 和 null），告诉我你加在哪。

在 L1 表单里（放在「探险地图」分组下面，新开一个「通关小组奖（可选）」分组）加：
1. 奖项名字 groupAwardName：单行文本框，placeholder 给个例子「勇敢探险家奖」。
   小字：「全队通关后颁发的奖。名字最好和这个探险主题呼应，比如『丛林勇士勋章』。留空就用默认奖名。」
2. 奖项图片 groupAwardImage：<input type="file" accept="image/*">，选图后 canvas 压到长边 ≤800、
   JPEG/PNG base64 存 groupAwardImage；显示缩略图预览 + 「移除」按钮（设回 null）。
   小字：「一张奖杯/奖牌/徽章图，和探险主题搭。留空就用默认奖杯。」
3. 保存 L1 时把这两个值写回 Level（没填=空串 / null）。

注意：不动导出逻辑、不改 schemaVersion（这两个字段会自然跟着 exportFull 导出，importSingleLevel/importFull
自然写回）。多写中文注释，函数 20-30 行内。

写测试清单 docs/test-checklists/V1-28-小组奖配置.md（桌面 Chrome + iPad Safari 两段）：
- 填奖名 + 传奖图 → 保存 → 重开值还在；F12 看 groupAwardImage 是压缩后 base64（长边 ≤800）
- 「移除」奖图 → groupAwardImage 变 null
- 只填奖名不传图 / 都留空 → 保存不报错
- 老 L1（没这俩字段）→ 打开不报错，显示空
- 导出该 L1 的 single-level JSON → 文件里能看到 groupAwardName/groupAwardImage（验证随导出）
- F12 Console 无红色错误

完成后（按顺序）：
1. shared/version.js 的 BUILD_TAG 改 'V1-28-小组奖配置 · <今天日期>'
2. 更新 PROGRESS.md（V1-28 完成）
3. 改了 .js 的话同步对应 HTML 的 ?v= 版本号
4. 单行英文 commit + push（纯编辑端，不升 SW）
```

**验证清单**：
- [ ] L1 表单新增 groupAwardName + groupAwardImage，值能存能读
- [ ] 奖图压到长边 ≤800（base64），可移除；可全留空
- [ ] 老 L1 不报错；随 single-level 导出一起带出
- [ ] 没动 schema / 导出逻辑
- [ ] 测试 checklist + version.js + PROGRESS 更新

---

## [ ] V1-29. 游戏端：多人版通关庆典 + 小组颁奖

**前置**：V1-27（要有 players/teamName）、V1-28（要有奖名/奖图，可空走默认）
**预计时间**：1.5-2 天
**类型**：游戏端

> 📌 在现有通关页（victory.html）上**增量**加两块：① 队伍身份展示（队名 + 全体头像）；② 撒花后揭晓**整队**的小组奖（奖名+奖图，没配走 App 内置默认）。**单人（players 为空）维持现状**，不显示队伍/颁奖区。不碰结算幂等、撒花、A 区成绩单错题本、B 区海报。

**【复制给 Claude Code 的指令】**

```
victory.html 加「多人队伍展示 + 小组颁奖」，仅多人（session.players 非空）显示，单人维持现状。
读 level.groupAwardName/groupAwardImage，没配走内置默认奖。动 play/pages/victory.html、play/service-worker.js。

【动手前先确认】
1. 读 victory.html：找到撒花、结算幂等（status=completed/endedAt 只写一次）、总分/用时/战绩、合影区
   分别在哪，确认新内容【加在它们旁边】、不动它们。
2. console.log：session.players / session.teamName（V1-27 写的）、level.groupAwardName /
   level.groupAwardImage（V1-28 写的）。读取一律容错：session.players || []、空就当单人。
3. 头像照片从 photos 表按 avatarPhotoId 取（和合影取法一致）。

1. 队伍展示（仅 session.players?.length）：
   - 在页面靠上（「🎉 通关啦!」附近）显示队名（teamName 空就「我们队」）。
   - 一排全体头像小圆：有 avatarPhotoId → 显头像图；没有 → 显「颜色块（player.color）+ 编号（playerNumber）」圆，
     和 V1-27 创建时同一种画法（可抽个小函数两处复用，别各画各的）。

2. 小组颁奖（仅多人）：
   - 撒花之后揭晓奖：奖图（level.groupAwardImage，没配 → 内置默认，用一个 🏆 emoji 或自带的默认奖杯图）
     从小放大弹出 + 一行「🎉 {队名} 获得【{奖名}】！」（奖名空 → 默认「通关纪念奖」）。
   - 一个简单揭晓动画即可（scale + 渐显，0.6-0.8 秒），别搞复杂。奖图下方再放一排全体头像呼应「人人有份」。
   - 颁的是【整队一枚】，不区分个人（个人奖在 V2）。

3. 单人（session.players 为空 / 老存档）：完全不显示队伍区和颁奖区，victory 维持现在的样子。

4. 海报（V1-18）本任务【先不改】，保持现状；队伍信息只在通关页屏幕上显示（避免改 Canvas 海报引入风险）。

5. service-worker.js：CACHE_VERSION +0.01 → v2.28（改了 victory.html 预缓存页）。

注意：不碰结算幂等 / 撒花 / 成绩单错题本 / 海报生成；读取全程容错；多写中文注释。

写测试清单 docs/test-checklists/V1-29-多人庆典颁奖.md（按 §5.7 三段结构）：
- 多人 + L1 配了奖名奖图 → 通关页有队名+头像；撒花后奖图弹出 +「XX队 获得【XX奖】」
- 多人 + L1 没配奖 → 走内置默认奖名/默认奖杯，不报错、不空白
- 多人但有人没拍头像 → 该人显「颜色块+编号」圆
- 单人打一局 → 通关页和以前完全一样，无队伍/颁奖区
- 刷新通关页 → 用时不变（结算幂等没被破坏）、颁奖再演一次可接受
- 飞行模式（先缓存过 v2.28）颁奖正常
- iPad 三态

完成后（按顺序）：
1. shared/version.js 的 BUILD_TAG 改 'V1-29-多人庆典颁奖 · <今天日期>'
2. 更新 PROGRESS.md（V1-29 完成）
3. 改了 .js 的话同步 ?v=
4. 单行英文 commit + push（victory.html / service-worker.js / version.js / PROGRESS.md）
```

**验证清单**：
- [ ] 多人：队名 + 全体头像（含「颜色块+编号」fallback）显示
- [ ] 撒花后揭晓整队小组奖（奖名+奖图），没配走内置默认
- [ ] 单人维持现状，无队伍/颁奖区
- [ ] 不破坏结算幂等/撒花/成绩单/海报；SW 升 v2.28
- [ ] 测试 checklist + version.js + PROGRESS 更新

---

## [ ] V1-30. 数据落地 + 续玩 / 导入导出兼容回归

**前置**：V1-27、V1-29
**预计时间**：0.5-1 天
**类型**：共享层 + 游戏端（收口与回归，多为验证 + 少量补丁）

> 📌 把多人字段在「续玩、导入导出、老存档」三条路上的兼容性收口。多人玩家数据是**游戏端 session 本地数据**（不进 admin 导出）；只有 L1 上的奖名/奖图随 L1 导出。本任务主要是**回归 + 容错补丁**，不是新功能。

**【复制给 Claude Code 的指令】**

```
收口多人字段的兼容性，做回归 + 必要的容错补丁。不加新功能。

【要逐条确认/修的点】
1. 数据模型注释：shared 里 GameSession 的类型注释（types.js 之类）确认 players/teamName 已写明用途；
   Level 注释补上 groupAwardName/groupAwardImage。仅注释，不改逻辑。
2. 续玩（select-level.html 续玩横幅 + 按 session 状态跳 hint/quiz/photo）：
   - 多人打到一半退出 → 续玩回到原处，session.players/teamName 仍在（它们就在 session 对象里，
     存 IndexedDB 自然持久）。验证续玩不因 players 报错。
3. 通关页/各页读取容错：全局确认凡读 session.players 的地方都用 (session.players || [])，
   老存档（MVP/单人，无 players 字段）不报错。
4. 导入导出：
   - admin 导出 L1（single-level / full）→ groupAwardName/groupAwardImage 跟着出（exportFull 整对象倒出，
     V1-28 已验证一次，这里再确认 importSingleLevel/importFull 写回后编辑端能读到）。
   - 游戏端导入后开一局多人 → 颁奖能读到导入进来的奖名奖图。
   - 玩家头像（photos type:'avatar'）是游戏端本地数据，【不进】admin 导出，确认导入不会误删/误带。
5. 如发现任何读 players/teamName/groupAward* 没容错而报错的地方，补 || 默认值；除此之外不改逻辑。

若本轮改了任何 play 预缓存页：service-worker.js CACHE_VERSION +0.01 → v2.29；纯注释/不改 play 页则不升。

写测试清单 docs/test-checklists/V1-30-多人数据兼容.md（按 §5.7 三段结构）：
- 多人打到第 2 站退出 → 续玩回到第 2 站，通关后队伍/颁奖正常
- 导入一个【MVP 老库】→ 单人开局照常、通关页无多人区、不报错
- 编辑端配过奖的 L1 导出 → iPad 导入 → 多人通关能看到该奖
- 导入新库后老的多人存档若被清（importFull 清 sessions 是预期）→ 有提示、不崩
- iPad 三态

完成后（按顺序）：
1. shared/version.js 的 BUILD_TAG 改 'V1-30-多人数据兼容 · <今天日期>'
2. 更新 PROGRESS.md（V1-30 完成）
3. 单行英文 commit + push
```

**验证清单**：
- [ ] 多人续玩回到原处，players/teamName 持久
- [ ] 所有读 players 处容错，老存档/单人不报错
- [ ] L1 奖名奖图随导出导入正确流转；头像不进 admin 导出
- [ ] （如改 play 页）SW 升 v2.29
- [ ] 测试 checklist + version.js + PROGRESS 更新

---

## [ ] V1-31. D 区联调 + 真机三态验收

**前置**：V1-27 ~ V1-30 全部完成
**预计时间**：测试半天 + 修补 1 天
**类型**：阶段验收

**【你要做的事】**
1. 编辑端：给某 L1 配「通关小组奖」（奖名 + 一张和主题搭的奖图），导出 → AirDrop → iPad 导入。
2. iPad 上完整打一局**多人**（3 人，1 人拍头像、2 人用颜色块+编号）：
   - [ ] 人数选 3 → 玩家创建（名字/编号/颜色/可选头像）→ 队名 → 开始
   - [ ] 答题流程和单人一样（共享总分，不问「谁答对」）
   - [ ] 通关页：队名 + 全体头像；撒花后整队小组奖揭晓（奖名+奖图）
   - [ ] 全程飞行模式跑通（先联网缓存一次）
3. 再打一局**单人**：确认全程和 C 区时一模一样，没有任何多人 UI。
4. 续玩：多人打一半退出 → 回到原处 → 通关颁奖正常。
5. 回归：A 区成绩单错题本 / B 区地图故事海报 / C 区暂停·填空·随机抽题 都没被 D 区破坏。
6. 全部通过 → commit + push → 更新 PROGRESS.md（**D 区完成里程碑**）。
7. 决定是否发布给孩子：按 `docs/双分支发布说明.md` 把 main 同步到 release（记得升 CACHE_VERSION）。

**验证清单**：
- [ ] 多人全流程（创建→答题→颁奖）iPad 三态全通
- [ ] 单人零改动确认
- [ ] 续玩 + A/B/C 区功能回归无破坏
- [ ] PROGRESS.md 标记 D 区完成
- [ ] （可选）已决定是否发版给孩子

> ✅ D 区做完后，进入 E 区（真机户外验收）。个人积分/归属/投票颁奖等留待 V2 评估。

---

# 🟪 E 区：真实测试 + 修 bug(V1-32 ~ V1-35，提纲)

- **V1-32** 带孩子户外完整测一关(若做了多人则多人测)
- **V1-33** 收集反馈、修关键 bug、整理 known-issues
- **V1-34** 写 V1 完成里程碑 docs/MILESTONE_V1.md
- **V1-35** 决定 V2 是否启动(应急通关裁判模式 + 个人积分/归属/投票颁奖等)

---

## 🔗 相关文档
- `CLAUDE.md`(v1.9)、`docs/PRD.md`(v1.9)、`docs/PROGRESS.md`
- `docs/test-checklists/`、`docs/出题指南.md`(V1-07 新建)
- `docs/MILESTONE_MVP.md`、`docs/known-issues.md`、`docs/MVP_summary.md`

---

## 更新记录
- **v1.9.3(D 区简化为合作式轻量多人 + 展开)**(2026-06-24):① 原「个人积分归属 / 难度加成(×1.2/×1.5)/ 连续答对计数 / 答题之星 / 投票颁奖 / 保底奖」**全部推到 V2**;② V1 多人定为**合作式轻量版**——开局创建玩家(头像可选+名字+数字编号+6 色选一,无头像用「颜色块+编号」)+ 队名(可选);**答题流程零改动**(全队共享一个总分,不选「谁答对」);通关给**整队**颁一枚奖;③ 奖项**每个 L1 自配**(groupAwardName + groupAwardImage,可空走 App 内置默认),人人有份故**取消保底奖**;④ D 区由提纲展开为 V1-27~V1-31 详细任务(玩家创建 / L1 小组奖配置 / 多人庆典+颁奖 / 数据兼容 / 联调),从 8 个瘦到 5 个;⑤ E 区顺延 V1-32~V1-35,全表收口 V1-01~V1-35;⑥ 全区追加字段不升 schema(沿用 1.1 + 放宽校验);单人玩法零改动;⑦ Level 新增 groupAwardName/groupAwardImage,GameSession 启用 players/teamName(voteRecords/awards 留 V2),customAwards 留 V2。
- **v1.9.2(题型并入 C 区)**(2026-06-23):① 填空题做完(V1-24);② 原 D 区「多题型增强」判定为已覆盖需求(判断 MVP 已做 + 填空 V1-24),不再单列;③「标签随机抽题」从原 D 区 V1-34 前移为 C 区 V1-25(复用现有筛选,无 tags 字段),原 C 区联调验收顺延为 V1-26;④ D 区拆掉题型后变为纯多人,重排为 V1-27~V1-34;E 区顺延 V1-35~V1-38;全表流水码收口 V1-01~V1-38;⑤ 多选题如以后需要再单列(可能进 V2)。
- **v1.9.1(C 区展开 + 双分支)**:① C 区提纲展开为 V1-21~24 详细任务(主动暂停按钮 / 题目插图 / 单 L1 导出+追加导入 / 联调);② C 区全区不升 schema(V1-19 已到 1.1 且放宽校验);③ 新增双分支双部署(release 给孩子 / main 开发版「寻宝-DEV」),发布与同步流程见 `docs/双分支发布说明.md`,C 区开发期间孩子 release 不受影响;④ 总览表 A/B 区标完成、C 区进行中。
- **v1.9**:① 改用全 V1 连续流水码 V1-01 ~ V1-38(分区不再单独计数);② 成就海报从 A 区移到 B 区,一次到位做含地图版(取消无地图 v1,免返工);③ 故事(L1 开场/结局 + L2 三段文案)全部收进 B 区,和地图一起;④ 多人 + 多题型 + 随机抽题合并为 D 区并放到最后,标「待定」;⑤ 扫码/通关音效默认开,取消 App 内音效开关;⑥ 拼音任务补性能护栏(整段一次转、只转当前题);⑦ 单 L1 导入冲突简化为「覆盖确认」;⑧ 地图/插图图片上传统一压到 ≤1600px;⑨ 放宽 validateJson 明确写进 V1-19。
- **v1.8**:错题本改为全部通关后按站汇总;明确 V1-B 起放宽 schemaVersion 校验。

---

**A、B 区已完成;C 区 V1-21~V1-25 已完成并发版给孩子,剩 V1-26 联调。接着按顺序啃 D 区(合作式轻量多人):V1-27 → V1-28 → V1-29 → V1-30 → V1-31。一个做完真机过三态再下一个。** 🚀
