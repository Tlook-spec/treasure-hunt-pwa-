# 功能名：V1-30 多人字段兼容性收口（回归 + 容错）

> 不加新功能。确认多人字段（`session.players`/`teamName`、`Level.groupAwardName`/`groupAwardImage`、
> 头像 `photos type:'avatar'`）在续玩、导入导出、单人/老存档场景下不报错。
> 本轮**只改注释**（`shared/models/types.js`）+ 验证，未改 play 页 → **不升 CACHE_VERSION**。

> 代码核查结论（实现确认，非测试项）：
> - 全局只有 `victory.html` 读 `session.players`/`teamName`，均已 `(session.players || [])` / `(session.teamName||'')` 容错。
> - 续玩（`select-level.html`）不读 players，players 随 session 存 IndexedDB 自然持久。
> - `exportFull`/`exportSingleLevel` 只倒 levels/points/questions（整对象，含 groupAward*）；**photos 不导出**（头像不进 admin 导出）。
> - `importFull` 只清 levels/points/questions（不动 photos）；游戏端导入另清 sessions（M18，有未完成确认）。

---

## 桌面浏览器测试（Windows + Chrome）

### 续玩不因 players 报错
- [ ] 多人开局（≥2 人，填队名/头像）→ 打到第 2 站中途退出（关标签/回首页）
- [ ] 重新进入 select-level → 续玩横幅出现「继续上次的『XX』？已到第 2 站」
- [ ] 点「继续」→ 回到第 2 站原处（hint/quiz/photo 之一），不报错
- [ ] F12 查该 session：`players` 仍在、`teamName` 仍在（IndexedDB 持久）
- [ ] 续玩打到通关 → 通关页队伍展示 + 颁奖正常

### 单人 / MVP 老库不报错
- [ ] 导入一个【MVP 老库 JSON】（无多人字段的旧数据）→ 导入成功
- [ ] 单人开一局（人数 1）→ 全程正常，通关页**无队伍区、无颁奖区**
- [ ] F12 查 session：`players` 为 `[]`（新建单人）；老存档无字段时通关页也不报错
- [ ] F12 Console 全程无红色错误

### 配过奖的 L1 导出 → 导入 → 颁奖读到
- [ ] 编辑端给某 L1 配奖名 + 奖图（V1-28）→ 导出 single-level（和 full 各试一次）
- [ ] 打开导出 JSON：`data.levels[0]` 含 `groupAwardName` + `groupAwardImage`
- [ ] 游戏端导入该 JSON → 多人打通 → 颁奖显示的是**导入进来的**奖名 + 奖图
- [ ] 换一个没配奖的 L1 → 多人通关走默认「通关纪念奖」+🏆，不报错

### 导入清存档的提示（importFull 清 sessions 是预期）
- [ ] 有一个多人未完成存档时，去设置页「整库导入」→ 弹未完成游戏确认提示
- [ ] 确认导入 → 旧 sessions 被清（含那个多人存档）→ 不崩、不报错
- [ ] 导入后 select-level 不再显示那条续玩横幅（存档已清）

### 头像数据不被导入误删/误带
- [ ] 多人玩过、photos 表有 type='avatar' 记录 → 做一次整库导入
- [ ] 导入后 photos 表里的 avatar 记录**仍在**（importFull 不动 photos）；不报错
- [ ] admin 端导出的 JSON 里**没有** photos / 头像数据（只 levels/points/questions）

---

## iPad Safari 测试
- [ ] 多人中途退出 → Safari 重开 → 续玩回原处，通关队伍/颁奖正常
- [ ] 导入老库 → 单人一局正常，无多人区
- [ ] AirDrop 传入配过奖的 JSON → 导入 → 多人通关看到该奖

## iPad PWA 主屏幕图标测试（三态）
- [ ] 有网 Safari：同上全通
- [ ] PWA 有网：多人续玩 + 颁奖正常
- [ ] PWA 飞行模式（已缓存）：多人续玩、单人、颁奖均正常，Eruda 无红色错误
  - 注：本轮无 play 页改动、未升 SW，设置页 BUILD_TAG 可能仍显示上一个版本（属正常，下次 play 发版会刷新）

---

> **说明**：本任务是兼容性收口，理想结果是「什么都不用改也不报错」。代码核查已确认读取处都有容错；
> 如真机发现任何读 players/teamName/groupAward* 报错处，补 `|| 默认值` 即可，不改其他逻辑。
